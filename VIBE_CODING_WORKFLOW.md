# Vibe Coding 工作流程

**适用范围：** 本仓库的需求、设计、实现、测试、排障、文档和交付工作。
**分支规则：** 只使用 `main`；所有改动直接提交到 `main`，不得创建或切换其他分支。
**事实来源：** PRD、PDD、`Requirements/`、`Decisions/` 和 `Progress/`。`DOCUMENT_MAP.md` 只负责导航。

## 1. 开始任务

1. 运行 `git status --short --branch`，确认当前分支为 `main`，并识别未提交的用户改动。
2. 阅读 `AGENTS.md`、`DOCUMENT_MAP.md`、PRD、PDD、相关需求记录、决策记录和进度记录。
3. 确认本次请求的类型：缺陷、功能、功能改进、体验改进或仅打包。
4. 需要新产品能力、流程或边界时，先在 `Requirements/` 建立记录；影响架构、数据、权限、安全或平台边界时，再建立 `Decisions/` 记录。
5. 发现需求、设计或已有实现相互矛盾时，停止扩大实现范围，在记录中写明冲突和需要确认的决定。

## 2. 定义本次交付

每项工作开始前，把以下内容写进对应需求或进度记录：

- 用户可感知的结果和验收条件。
- 涉及的 PRD、PDD、接口、数据、权限、存储和平台边界。
- 不在本次范围内的能力，以及不能采用的绕过方式。
- Go 服务端与 Vite 前端构建检查、所需真实环境和待补证据。

不把待定设计、模拟验证或本地检查写成已上线行为。POC 只能证明已经实际覆盖的范围。

## 3. 实现和同步

1. 在已确认边界内做最小完整改动，保留无关的用户改动。
2. 代码、配置和文档在同一轮同步更新。产品行为和用户流程更新 PRD 与需求记录；架构、接口、数据、权限、运行时或兼容性更新 PDD 和决策记录；阶段位置更新进度记录。
3. 使用仓库已有的抽象和平台接口。禁止通过宿主机路径、跨用户参数、临时高权限或写入目标数据来绕过懒猫租户边界。
4. 每次实现都直接在 `main` 完成。不能用分支隔离工作，也不能用分支规避当前工作区的未提交改动。

## 4. 验证和记录证据

1. 本地检查只编译 Go 服务端和构建 Vite 前端；不新增、修改、生成或运行测试、验证代码和测试专用 fixture。
2. 涉及懒猫权限、多实例隔离、网盘写入或运行时投影时，必须在真实平台验证；本地构建检查不能替代平台证据。
3. 在进度记录中写明执行过的构建命令、结果、环境、未执行检查和原因。没有运行的检查不得标记为通过。
4. 平台、权限、运行时投影或源解析器改变后，重新执行 `docs/APPVAR_READ_POC_RUNBOOK.md` 的两用户回归矩阵。

## 5. 交付和收尾

1. 交付前再次运行 `git status --short --branch`，确认仍在 `main`，只说明本次改动影响的文件。
2. 更新 `DOCUMENT_MAP.md`，让新增或调整后的需求、决策、进度和关键实现入口可被定位。
3. 在交付说明中写清已完成内容、改动文件、关联记录、已执行验证、剩余风险和需要用户决定的事项。
4. 未获明确授权，不推送、发布、部署或创建合并请求。

## 6. 当前项目的固定边界

- 备份应用和目标数据都遵循当前用户、当前多实例容器的租户边界。
- 不使用 `other_uid`、`appvar.other.write`、`compose.override`、宿主机挂载或跨用户回退。
- 长期备份只写入当前用户自己的懒猫网盘；目标应用数据只读。
- 已通过的 POC 数据入口为 `/lzcapp/run/data/app/var`。平台、权限或解析器变化后，先完成回归验证，再依赖该入口继续开发。
