import { AlertTriangle, Box, CheckCircle, File, FileText, Folder, FolderTree, X } from 'lucide-react';

export function GlobalModals({ activeModal, modalPayload, setActiveModal }) {
  return <>
    {activeModal === 'unsupportedDb' && modalPayload && <div className="fixed inset-0 z-50 bg-slate-900/50 backdrop-blur-xs flex items-center justify-center p-4"><div className="bg-white border border-slate-200 rounded-2xl max-w-lg w-full p-6 space-y-4 shadow-xl"><div className="flex items-start space-x-3 text-rose-600"><div className="p-2 bg-rose-50 rounded-xl border border-rose-100"><AlertTriangle className="w-6 h-6" /></div><div><h3 className="text-base font-bold text-slate-900">检测到不支持的服务型数据库</h3><p className="text-xs text-rose-600">触发安全阻断保护机制</p></div></div><div className="bg-slate-50 p-3.5 rounded-xl border border-slate-200 text-xs space-y-2"><div className="flex justify-between"><span className="text-slate-500">应用名称：</span><span className="font-semibold text-slate-900">{modalPayload.appName || modalPayload.name}</span></div><div className="flex justify-between"><span className="text-slate-500">Deploy ID：</span><span className="font-mono text-emerald-600">{modalPayload.deploy_id || 'shared'}</span></div><div className="flex justify-between"><span className="text-slate-500">数据库特征：</span><span className="text-rose-600 font-semibold">{modalPayload.unsupportedDb || 'MySQL / PostgreSQL / Redis / MongoDB'}</span></div></div><p className="text-xs text-slate-600 leading-relaxed">懒猫应用备份 V1 专为普通文件与标准 SQLite 3 设计。直接冷复制正在运行的服务型数据库物理文件会导致文件破坏。根据 V1 安全边界，系统严格阻止为此类实例创建无效快照。</p><div className="flex justify-end pt-2"><button onClick={() => setActiveModal(null)} className="px-4 py-2 bg-slate-900 text-white rounded-xl text-xs font-semibold hover:bg-slate-800 transition">我知道了</button></div></div></div>}
    {activeModal === 'noData' && <div className="fixed inset-0 z-50 bg-slate-900/50 backdrop-blur-xs flex items-center justify-center p-4"><div className="bg-white border border-slate-200 rounded-2xl max-w-md w-full p-6 space-y-4 shadow-xl"><div className="flex items-center space-x-3 text-slate-700"><div className="p-2 bg-emerald-50 rounded-xl"><Box className="w-5 h-5 text-emerald-600" /></div><h3 className="text-base font-bold text-slate-900">该应用无需要备份的数据</h3></div><p className="text-xs text-slate-600 leading-relaxed">纯前端应用或初始 `appvar` 目录为空的应用无需产生物理归档。这不会计入系统的未保护风险，也不产生错误日志。</p><div className="flex justify-end"><button onClick={() => setActiveModal(null)} className="px-4 py-2 bg-emerald-600 text-white rounded-xl text-xs font-semibold hover:bg-emerald-700 transition shadow-xs">关闭</button></div></div></div>}
    {activeModal === 'fileIndex' && modalPayload && <div className="fixed inset-0 z-50 bg-slate-900/50 backdrop-blur-xs flex items-center justify-center p-4"><div className="bg-white border border-slate-200 rounded-2xl max-w-2xl w-full p-6 space-y-4 shadow-xl"><div className="flex justify-between items-center border-b border-slate-200 pb-3"><div className="flex items-center space-x-2"><FolderTree className="w-5 h-5 text-emerald-600" /><h3 className="text-sm font-bold text-slate-900">快照文件索引预览 ({modalPayload.appName})</h3></div><button onClick={() => setActiveModal(null)} className="p-1 text-slate-400 hover:text-slate-600"><X className="w-5 h-5" /></button></div><div className="bg-slate-900 text-slate-100 rounded-xl p-3.5 font-mono text-xs max-h-80 overflow-y-auto space-y-1.5 shadow-inner"><div className="text-emerald-400 flex items-center"><Folder className="w-4 h-4 mr-1.5 text-emerald-400" /> / (appvar root)</div><div className="pl-4 text-teal-400 flex items-center"><FileText className="w-4 h-4 mr-1.5 text-teal-400" /> sqlite/notus_main.sqlite <span className="text-slate-400 ml-2">(380MB, Online Snapshot Copy)</span></div><div className="pl-4 text-slate-300 flex items-center"><File className="w-4 h-4 mr-1.5 text-slate-400" /> config.json <span className="text-slate-500 ml-2">(2.4KB)</span></div><div className="pl-4 text-emerald-300 flex items-center"><Folder className="w-4 h-4 mr-1.5 text-emerald-400" /> uploads/</div><div className="pl-8 text-slate-300 flex items-center"><File className="w-4 h-4 mr-1.5 text-slate-400" /> avatar.png <span className="text-slate-500 ml-2">(145KB)</span></div><div className="pl-8 text-slate-300 flex items-center"><File className="w-4 h-4 mr-1.5 text-slate-400" /> doc_metadata.json <span className="text-slate-500 ml-2">(18KB)</span></div><div className="pl-4 text-emerald-300 flex items-center"><CheckCircle className="w-4 h-4 mr-1.5 text-emerald-300" /> checksums.sha256</div><div className="pl-4 text-emerald-400 flex items-center"><FileText className="w-4 h-4 mr-1.5 text-emerald-400" /> manifest.json</div></div><div className="flex justify-between items-center text-xs text-slate-500 pt-1"><span>* 保护应用隐私安全，不在控制台展示敏感文件文本内容</span><button onClick={() => setActiveModal(null)} className="px-4 py-2 bg-slate-100 hover:bg-slate-200 text-slate-700 rounded-xl font-semibold transition">关闭</button></div></div></div>}
  </>;
}
