# Progress — 选择范围文件树与应用元数据展示

**Requirement:** REQ-20260831-009
**Status:** 本地实现完成；真实平台验证待执行
**Last updated:** 2026-08-31

## 本次实现

- 向导范围目录从扁平列表改为可展开树；目录选择递归填充当前目录树中的后代路径，取消目录会一并清除后代选择。目录、普通文件和 `.db`/SQLite 使用统一颜色体系中的文件夹、文件、数据库图标。
- 计划详情把范围模式本地化为“全量备份 / 自定义备份范围”，自定义范围与向导共享树组件；目标实例显示应用名称和图标，目录树读取已保存范围并在可用时补充当前安全目录。
- 任务中心列名改为“应用”，任务、批次、快照和备份库优先使用当前租户目录的真实图标和名称。
- 服务端在计划、任务和快照响应中按 `deployId` 补充目录元数据；目录 API 缺少商店标题时生成可读降级名称，缺少图标时根据 OIDC box 域名生成系统图标地址。前端同时处理无效/混合内容图标并回退为首字母。

## 本地验证

- `npm run api:generate --prefix apps/web`：通过。
- `cd apps/web && npx vite build`：通过。
- `go build -o /tmp/mimi-backup-server ./apps/server/cmd/server`：通过。
- `git diff --check`：通过。
- 未运行测试，未安装、部署、发布、推送或创建合并请求。

## 待补证据

- 在真实懒猫设备确认未上架应用的系统图标 URL、目录树完整分页和当前用户元数据；确认平台图标缺失时首字母降级可读。
