# 咪咪应用备份需求文档 PRD

> 文档版本：V1  
> 产品名称：咪咪应用备份（Mimi App Backup）
> LPK 包标识：`cloud.lazycat.app.mimi-app-backup`
> 产品形态：安装在懒猫微服中的多实例 Web 应用
> 目标平台：懒猫微服 LPK V2  
> 登录方式：懒猫 OIDC
> 支持语言：简体中文（zh-CN）、English（en-US）  
> 最后更新：2026-09-01
> 说明：文档版本固定为 V1，后续调整只更新“最后更新”和变更记录。

## 1. 产品背景

懒猫微服中的应用会把配置、附件、索引、SQLite 数据库和运行状态保存在应用数据目录中。应用升级、程序缺陷、误操作或存储异常都可能造成数据丢失。Notus 的向量索引丢失并需要重新构建，是本项目最直接的使用场景。

咪咪应用备份为当前登录用户发现懒猫中全部已安装的应用实例，执行手动或定时备份，并将结果保存到该用户自己的懒猫网盘。管理员和普通用户使用同一份应用目录；计划、任务、快照索引和网盘仍按登录用户的备份应用实例隔离。

项目已完成 POC，当前链路可以：

1. 识别当前懒猫用户。
2. 查询当前账号可使用的全部已安装应用目录。
3. 读取 `appvar.other.read` 对应的运行时应用数据投影。
4. 探测应用数据目录、文件数量、容量和数据库类型。
5. 对普通文件计算 SHA-256。
6. 为符合条件的应用创建一次手动备份。
7. 将备份结果写入当前用户自己的懒猫网盘。

V1 在该链路上增加 OIDC 会话、正式页面、备份计划、任务执行记录、ZIP 归档、SQLite 一致性快照、备份库、告警和保留策略。任务调度队列属于后端实现，不作为用户侧功能展示。

## 2. 产品定位

V1 是面向懒猫用户的应用数据备份工具，覆盖以下数据：

- 普通文件和目录。
- 配置文件、附件、图片、模型文件、向量索引和全文索引。
- 标准 SQLite 3 数据库。
- 当前用户在懒猫平台上下文中可访问的单实例和多实例应用数据。

V1 不承担整机备份、容器镜像备份和通用数据库备份。

## 3. 产品原则

### 3.1 当前用户边界

普通用户和管理员登录后都可以查看、探测和使用平台返回的全部已安装应用实例。应用安装者不是目录过滤条件，服务端不接受浏览器提交的 UID 或 owner 作为授权参数。

角色差异只保留在账户标识和后续预留管理能力：每个登录用户仍只能看到自己的计划、任务、快照索引和网盘目录，备份结果写入自己的懒猫网盘。

### 3.2 多实例备份应用

咪咪应用备份自身使用多实例部署。每个懒猫用户运行独立的应用容器，并拥有独立的：

- 控制数据库。
- 备份计划。
- 任务中心中的批次与任务记录。
- 快照索引。
- 告警和设置。
- 懒猫网盘备份目录。

### 3.3 单实例和多实例目标应用

V1 支持平台返回的全部已安装单实例与多实例目标应用。

多实例目标应用由平台为不同用户运行独立实例，页面标记为“用户隔离”。

单实例目标应用可能使用共享 `appvar`。V1 允许备份，并在详情和计划中显示“共享实例风险”提示；提示不阻断查看任务、立即备份或创建计划。

平台返回的已安装应用目录和运行时投影构成 V1 的授权边界。产品不使用 `other_uid`，也不按应用安装者过滤；控制库、备份任务和网盘写入仍由当前登录会话隔离。

### 3.4 数据只读

备份过程只读取目标应用数据。V1 不申请 `appvar.other.write`，不提供写回、覆盖或删除目标应用数据的能力。

### 3.5 用户网盘存储

所有长期备份文件写入当前用户自己的懒猫网盘。备份应用容器只保存任务状态、索引和短生命周期临时文件。

## 4. V1 产品目标

1. 所有业务页面必须经过懒猫 OIDC 登录。
2. 登录用户可以看到并使用全部已安装应用实例。
3. 用户可以查看应用数据大小、文件数量、数据库类型和可备份状态。
4. 用户可以立即备份单个应用。
5. 用户可以批量选择多个应用创建一次性备份。
6. 用户可以创建按小时、每天、每周或 Cron 执行的备份计划。
7. 普通文件在应用运行期间进行受控读取。
8. SQLite 在应用运行期间创建一致性快照。
9. MySQL、PostgreSQL、MongoDB、Redis 等数据库被检测后阻止备份。
10. 每次备份生成 `snapshot.zip` 和 `manifest.json`。
11. 备份目录固定为 `MimiAppBakcup/<deploy_id>/<时间>/`。
12. 用户可以查看任务进度、历史结果、失败原因和快照完整性。
13. 系统保留快照校验和受限的服务端导出能力，不直接恢复到目标应用；当前 V1 快照详情不提供导出入口。
14. 后端可以在同一计划时间快速创建大量任务，并使用有界并发稳定执行。
15. 中文和英文页面、状态、错误和通知保持一致。

## 5. 成功标准

- Notus 可以建立每天自动备份计划。
- Notus 普通文件、索引和 SQLite 数据能够写入当前用户网盘。
- `snapshot.zip` 可以正常解压，内部文件路径与源应用相对目录一致。
- 外部 `manifest.json` 可以独立描述快照、归档校验值和源应用信息。
- 单实例应用在备份前展示共享实例风险。
- 多实例应用展示平台返回的每个已安装部署实例。
- 普通用户和管理员都可以使用全部已安装应用；计划、任务、快照索引与网盘内容仍按当前登录用户隔离。
- 数据库不支持、无数据、权限不足和系统应用均有明确状态。
- 应用重启后可以恢复任务执行状态，并按补跑规则处理错过的计划。
- 同一时间点创建大量任务时，不出现无界 goroutine、内存陡增或大量文件同时打开。

## 6. V1 非目标

- 跨租户控制库、计划、任务、快照索引或网盘管理。
- 直接恢复到目标应用 `appvar`。
- 自动停止、启动或重启目标应用。
- MySQL、MariaDB、PostgreSQL、MongoDB、Redis 的正式备份。
- Elasticsearch、OpenSearch、ClickHouse 等服务型数据库备份。
- DuckDB、LMDB、RocksDB、LevelDB、Pebble、bbolt、BadgerDB、H2、HSQLDB、Derby 的一致性备份。
- LightOS 系统盘备份。
- 懒猫网盘、懒猫相册等系统级数据的通用备份。
- 其他应用的用户文稿、媒体目录、RemoteFS、NAS 和外部存储。
- 整机、块设备、容器镜像和操作系统配置备份。
- 本地账号、密码登录和游客模式。
- POC 探测页面作为正式产品页面。
- 任意宿主机路径输入。

## 7. 用户角色和权限

### 7.1 普通用户

普通用户可以：

- 使用懒猫 OIDC 登录自己的备份应用实例。
- 查看平台返回的全部已安装应用和实例。
- 查看应用可备份性。
- 执行立即备份。
- 创建和管理自己的备份计划。
- 查看自己的任务、快照、存储和告警。
- 修改自己的备份设置。
- 查看快照的创建时完整性记录；当前 V1 快照详情不提供校验或导出入口。

### 7.2 管理员

管理员在 V1 中与普通用户拥有相同的全量应用目录和备份功能。管理员角色用于：

- 显示账户角色。
- 记录审计信息。
- 预留后续管理员能力。

管理员不能：

- 使用 `other_uid` 或浏览器提交的 UID 查询目录。
- 切换会话租户。
- 查看其他用户的计划、任务、快照和网盘目录。
- 代表其他用户创建计划。

### 7.3 OIDC 会话

所有业务页面必须存在有效 OIDC 会话。进入业务页面后，系统校验：

```text
session.gateway_uid
=
X-HC-User-ID
```

`session.uid` 是 OIDC UserInfo 的 `preferred_username`，用于显示和审计；它不与懒猫网关 UID 作相等比较。回调保存当次网关注入的 UID，后续请求只使用同一命名空间的 `session.gateway_uid` 做校验。

任一身份缺失或不一致时：

- 清除本地会话。
- 返回 403。
- 显示“当前登录会话与懒猫账号不一致”。
- 不返回任何应用、任务或快照数据。

## 8. 数据支持矩阵

| 数据类型 | V1 行为 | 一致性 | 允许创建计划 |
| --- | --- | --- | --- |
| 普通文件和目录 | 写入 ZIP | 文件级、尽力一致 | 是 |
| JSON、YAML、TOML、CSV、Markdown、文本 | 写入 ZIP | 文件级、尽力一致 | 是 |
| 图片、附件、模型文件 | 写入 ZIP | 文件级、尽力一致 | 是 |
| 向量索引、全文索引、自定义二进制目录 | 写入 ZIP并提示运行时写入风险 | 文件级、尽力一致 | 是 |
| 标准 SQLite 3 | 使用 Online Backup API 后写入 ZIP | SQLite 快照一致 | 是 |
| SQLite WAL、SHM、Journal | 不单独复制 | 由 SQLite 快照覆盖 | 是 |
| SQLCipher、加密 SQLite | 阻止备份 | 不支持 | 否 |
| MySQL、MariaDB | 阻止备份 | 不支持 | 否 |
| PostgreSQL | 阻止备份 | 不支持 | 否 |
| MongoDB | 阻止备份 | 不支持 | 否 |
| Redis RDB、AOF | 阻止备份 | 不支持 | 否 |
| 其他已知文件数据库 | 阻止备份 | 不支持 | 否 |
| 未知数据库特征 | 阻止备份 | 不支持 | 否 |
| Socket、命名管道、设备文件 | 跳过并记录 | 不适用 | 不单独阻断 |
| 空数据目录 | 显示“无应用数据” | 不适用 | 否 |
| 系统级或特殊存储应用 | 显示“系统或权限受限” | 不适用 | 否 |
| LightOS 隔离助手数据（如 Totoro） | 显示“系统或权限受限”，说明 LightOS 数据暂不支持备份 | 不适用 | 否 |

## 9. 可备份性判定

应用满足以下条件时，状态为“可备份”：

1. 当前 OIDC 用户可以在应用目录中看到该应用。
2. 后端目录查询结果与当前 `tenant_uid` 一致。
3. 运行时数据投影能够解析该应用的 `appid` 与 `deploy_id`。
4. 数据目录可以读取。
5. 未发现阻断型数据库。
6. 数据目录至少包含一个允许备份的普通文件或 SQLite。
7. 目标应用需排除备份应用自身。

单实例应用满足上述条件后仍可备份，同时标记“共享实例风险”。

## 10. 业务对象

### 10.1 当前用户

字段包括：

- `tenant_uid`
- OIDC subject
- 显示名称
- 邮箱
- 角色：`NORMAL` 或 `ADMIN`
- 会话时间
- 当前语言
- 当前时区

### 10.2 应用

字段包括：

- 应用名称
- `appid`
- 版本
- 图标
- 是否内置
- 单实例或多实例
- 全部已安装实例数量
- 可备份状态
- 数据大小
- 文件数量
- SQLite 数量
- 最近备份时间
- 下次计划时间

### 10.3 应用实例

唯一标识：

```text
tenant_uid + appid + deploy_id
```

字段包括：

- `appid`
- `deploy_id`
- 实例模式
- 运行状态
- 数据目录状态
- 可备份性
- 数据类型摘要
- 数据大小
- 文件数量
- 最近检测时间

### 10.4 备份计划

字段包括：

- 计划 ID
- 计划名称
- 目标选择规则
- 执行频率
- 时区
- 备份窗口
- 并发策略
- 重试策略
- 通知设置
- 状态
- 下次执行时间

### 10.5 备份批次

同一个计划时间点形成一个批次。批次包含一个或多个实例任务，并共享 `scheduled_at`。

### 10.6 实例任务

一个任务只处理一个 `deploy_id`。任务拥有独立状态、执行时间、重试记录、输出路径和错误信息。

### 10.7 快照

快照由两个最终文件组成：

```text
snapshot.zip
manifest.json
```

`manifest.json` 写入完成后，该快照才进入备份库。

## 11. 信息架构

### 11.1 主菜单

| 菜单名称 | 英文名称 | 路由 | 页面目标 |
| --- | --- | --- | --- |
| 概览 | Overview | `/overview` | 查看当前用户保护状态、任务、风险和容量 |
| 应用 | Applications | `/applications` | 查看全部已安装应用、实例和可备份性 |
| 备份计划 | Backup Plans | `/plans` | 创建和管理定时备份 |
| 任务中心 | Task Center | `/tasks` | 查看任务历史和备份结果 |
| 备份库 | Backup Library | `/backups` | 浏览当前用户的历史快照 |
| 存储 | Storage | `/storage` | 查看当前用户网盘备份占用和维护状态 |
| 告警 | Alerts | `/alerts` | 查看权限、任务、数据库和存储异常 |
| 设置 | Settings | `/settings` | 配置备份、通知、语言、OIDC 会话和环境状态 |

### 11.2 非主菜单页面

- 首次使用向导：`/setup`
- 应用详情：`/applications/[appid]`
- 实例详情：`/applications/[appid]/instances/[deploy_id]`
- 新建计划：`/plans/new`
- 编辑计划：`/plans/[plan_id]`
- 批次详情：`/tasks/batches/[batch_id]`
- 任务详情：`/tasks/jobs/[job_id]`
- 快照详情：`/backups/[snapshot_id]`
- 文件索引：`/backups/[snapshot_id]/files`
- 告警详情：`/alerts/[alert_id]`
- OIDC 登录错误：`/auth/error`

POC 的“应用与数据库全量探测”页面保留在代码仓库中，供开发和排查参考，不进入生产导航、路由表和 PRD 功能范围。

## 12. 全局应用框架

### 12.1 左侧菜单

组件：

- 产品 Logo 和名称。
- 八个主菜单图标。
- 应用、备份计划、任务中心、备份库和告警入口旁显示当前租户范围内的总数，不受列表分页影响。
- 当前页面高亮。
- 折叠按钮。
- 移动端抽屉菜单。

正式页面容器最大宽度统一为可用内容区域的 90%；窄屏继续使用单列内容和移动端导航。

### 12.2 侧栏底部操作区

组件：

- 紧凑的当前用户头像、名称和角色入口。
- 账户与退出登录仍在设置页完成。

生产页面不保留顶部菜单栏；页面标题和操作由各页面头部承载，告警由左侧菜单入口进入。语言只在设置页的“外观”条目中修改，不在侧栏提供快捷切换。

### 12.3 公共组件

- `VirtualDataTable`：服务端分页和虚拟滚动。
- `FilterBar`：状态、类型、时间和大小筛选。
- `StatusBadge`：统一状态标签。
- `ModeBadge`：单实例、多实例。
- `DatabaseBadge`：SQLite、未知数据库、不支持数据库。
- `BulkActionBar`：批量立即备份和批量创建计划。
- `ConfirmDialog`：取消和需要确认的备份操作。
- `DetailModal`：在当前页面查看应用、计划、批次、任务和快照详情，并支持同一弹窗内的关联任务/快照切换。
- `SessionExpiredDialog`：OIDC 会话过期。
- `SharedInstanceRiskDialog`：单实例共享数据提示。
- `UnsupportedDatabaseDialog`：数据库阻断说明。
- `NoDataState`：无应用数据。
- `ErrorPanel`：错误码、追踪 ID、重试入口。

首次进入应用时，应用目录同步和应用数据准备期间显示轻量全局 loading；应用数据首次准备完成后显示一次站内提示。加载中的应用列表使用同步状态文案，不把尚未返回的数据误判为空列表。loading 支持移动端布局和 reduced-motion 偏好。

所有正式页面的表格表头使用独立底色、边界和不小于 12px 的可读字号，不以过小的大写装饰文本承载列名。

## 13. OIDC 登录流程

### 13.1 首次访问

1. 用户通过懒猫入口打开应用。
2. 后端检查 OIDC 会话。
3. 没有会话时显示应用登录页；该页不自动发起授权。
4. 用户点击“使用懒猫 OIDC 登录”后，后端创建登录事务并跳转懒猫 OIDC。
5. 用户完成授权后返回 `/auth/oidc/callback`。
6. 后端校验 `state`、`nonce`、授权码和 ID Token。
7. 后端读取用户信息和权限组。
8. 后端保存 OIDC 用户信息和回调请求的懒猫网关 UID，并创建会话。
9. 浏览器进入首页；后续业务请求校验 `session.gateway_uid` 与当前 `X-HC-User-ID`。

### 13.2 会话状态

侧栏账户入口和设置页展示：

- 用户名称。
- UID。
- `NORMAL` 或 `ADMIN`。
- 会话到期时间。
- 重新登录。
- 退出登录。

### 13.3 会话异常

- OIDC 回调失败：显示登录错误页。
- 用户身份不一致：清除服务端会话与 Cookie，回到登录页并提示重新使用当前实例所属的懒猫账号授权；不返回业务数据。
- 会话过期：弹窗后重新登录。
- 角色发生变化：刷新会话。
- OIDC 服务暂不可用：保留后台任务，前台显示服务异常。

## 14. 首次使用向导

首次使用向导只面向当前登录用户。

### 14.1 欢迎

展示：

- 支持普通文件和 SQLite。
- 不支持的数据库类型。
- 单实例共享数据提示。
- 备份只写入当前用户网盘。
- V1 不提供直接恢复。

组件：能力卡片、安全说明、开始按钮。

### 14.2 登录与身份检查

检查：

- OIDC 会话有效。
- 当前业务请求的 `session.gateway_uid` 与 `X-HC-User-ID` 一致。
- 显示用户角色。
- 管理员专属的集中管理功能未启用；应用目录对所有角色统一开放。

组件：身份卡片、状态列表、重新登录按钮。

### 14.3 权限检查

检查：

- `appvar.other.read`
- `document.write`
- 可选 `user.notify`
- 运行时兼容权限
- appvar 数据投影
- 当前用户网盘写入

组件：权限列表、重新检测、错误说明。

### 14.4 应用扫描

功能：

- 查询全部已安装应用。
- 排除备份应用自身。
- 扫描数据目录。
- 识别单实例与多实例。
- 识别 SQLite 和不支持数据库。
- 分类可备份、无数据、不支持和检测失败。

组件：扫描进度、统计卡、问题列表。

### 14.5 首次备份

功能：

- 选择一个可备份应用。
- 单实例时显示共享数据风险提示。
- 执行手动备份。
- 显示 ZIP 路径、SHA-256、文件数、大小和耗时。

组件：应用选择器、进度时间线、结果卡片。

## 15. 概览页

### 15.1 指标卡

- 全部已安装应用数。
- 可备份应用数。
- 已保护应用数。
- 待首次备份。
- 不支持应用数。
- 最近 24 小时成功、失败和警告任务。
- 排队和运行任务。
- 当前用户备份占用。

指标卡使用统一高度、内边距、圆角、图标底色和数值层级的四列网格；窄屏依次收敛为三列、两列和单列。

### 15.2 保护状态分布

状态：

- 正常保护。
- 待首次备份。
- 即将过期。
- 已过期。
- 未保护。
- 无数据。
- 不支持。
- 配置异常。

组件：环形图、图例、下钻筛选。

### 15.3 执行状态与吞吐

页面只展示服务端已提供的任务和批次状态。队列深度、队列位置、吞吐预测和资源限制不属于 V1 用户界面；队列仅作为服务端内部执行机制保留。

### 15.4 下一批计划

展示未来 24 小时：

- 计划名称。
- 执行时间。
- 目标应用数。
- 预计数据量。
- 上次结果。
- 操作：立即执行、暂停、查看。

### 15.5 风险提醒

- 尚未备份。
- 连续失败。
- 检测到不支持数据库。
- 单实例共享数据计划。
- 网盘空间不足。
- OIDC 配置异常。
- appvar 投影不可用。
- 存在未完成的临时任务。

### 15.6 最近活动

展示计划变更、备份开始、备份完成、校验、登录和权限变化。

## 16. 应用页

### 16.1 标签页

- 全部。
- 可备份。
- 已保护。
- 未保护。
- 无应用数据。
- 数据库不支持。
- 系统或权限受限。
- 需要重新检测。

“系统或权限受限”用于已知系统数据边界、LightOS 隔离助手数据、实际读取权限被拒绝的实例，以及达到受限扫描目录项上限的实例。“需要重新检测”只用于未分类的真实探测异常或超时；它不表示前端仍在加载数据。

### 16.2 应用列表字段

- 应用图标。
- 应用名称。
- `appid`。
- 版本。
- 单实例或多实例。
- 运行状态。
- 数据大小。
- 文件数量。
- SQLite 数量。
- 可备份性。
- 保护状态。
- 最近备份。
- 下次执行。

列表不单独展示 `deploy_id` 列；`deploy_id` 仍用于搜索、详情、计划目标以及任务和快照关联。

### 16.3 筛选

- 应用名称、`appid`、`deploy_id`。
- 单实例、多实例。
- 运行状态。
- 可备份性。
- 保护状态。
- 是否包含 SQLite。
- 数据库类型。
- 数据大小区间。
- 最近备份时间。

不提供用户、owner 和租户筛选。

### 16.4 行操作

- 立即备份。点击后先显示确认弹窗；不可备份实例保留禁用图标。
- 创建计划。
- 查看详情。
- 重新检测。

“查看任务”和“查看快照”收纳在“查看详情”弹窗中，和应用基本信息、保护状态一起展示；应用列表不再重复放置这两个图标。

用户点击“重新扫描全部”后，服务端接受应用目录刷新请求时，页面显示短时中英文站内提示，说明刷新已提交并正在同步；异步同步完成前不显示已完成措辞。

首次进入应用时，在应用目录同步及首屏应用数据准备期间显示全局 loading；数据就绪不自动显示 toast。应用目录仍在同步时，列表空状态显示同步文案，不显示“没有找到匹配的应用实例”。

### 16.5 批量操作

- 批量立即备份。
- 批量创建计划。
- 批量重新检测。

批量确认弹窗展示：

- 选中应用数。
- 实际可备份数。
- 无数据数。
- 数据库阻断数。
- 单实例风险数。
- 预计总大小。

## 17. 应用详情页

### 17.1 基本信息

- 应用名称、图标、`appid`、版本。
- `deploy_id`。
- 单实例或多实例。
- 运行状态。
- 最近同步时间。

### 17.2 数据概览

- 数据目录状态。
- 文件数量。
- 目录数量。
- 总大小。
- SQLite 数量。
- 跳过项。
- 数据库检测结果。
- 最近检测时间。

### 17.3 数据范围

- 目录树。
- 文件类型统计。
- 最大文件列表。
- SQLite 路径摘要。
- 忽略项。
- 不显示文件正文和源绝对路径。

### 17.4 保护状态

- 关联计划。
- 最近成功快照。
- 最近失败任务。
- 下次执行时间。
- 历史成功率。
- 快照容量趋势。

### 17.5 操作

- 创建或编辑计划。由单个应用入口打开时，计划目标固定为当前应用；从批量入口打开时才允许多选。
- 查看任务历史。
- 查看备份库。

应用详情弹窗只呈现应用信息、保护状态和关联历史；立即备份与重新检测只从应用列表发起，避免在详情中重复放置执行入口。

单实例应用在页面顶部展示共享数据风险卡片。

## 18. 备份计划页

### 18.1 计划列表字段

- 计划名称。
- 目标类型。
- 目标应用数。
- 状态。
- 执行频率。
- 下次执行。
- 备份窗口。
- 最近结果。
- 最近结果为失败时的可读失败原因；多目标计划可展示多个原因。用户界面不显示稳定错误码。
- 连续失败次数。

### 18.2 操作

- 新建。
- 查看。
- 立即执行。
- 暂停或启用。
- 编辑。

除“新建”外，上述操作使用带可访问名称的图标表达，避免计划列表被重复文字按钮占满。

### 18.3 信息层级

页面先列出计划定义，再展示最近执行。计划行突出名称、范围、频率、状态和下次执行；立即执行、暂停/启用和编辑收敛在同一操作区。最近执行只显示有限条批次，供用户快速确认计划有没有按预期运行。

## 19. 新建或编辑计划向导

### 19.1 第一步：选择备份模式

新建和编辑计划只提供两种模式：

- 完整备份：归档当前应用数据根下全部可支持的普通文件和标准 SQLite。
- 自定义范围：由用户选择目录和单文件。

历史 `CORE` 计划继续可读和可执行，但不再出现在新建或编辑界面中。立即备份始终按完整备份执行。

### 19.2 第二步：添加应用

计划只接受当前可备份的显式应用实例，不包含未来新出现的应用。用户可搜索应用、查看应用图标，并一次添加多个目标；界面展示已选目标摘要和每个目标的预检结果，包括普通文件、SQLite、不支持数据库、无数据、单实例共享风险、预计文件数和大小。

### 19.3 第三步：配置范围

每个目标分别配置完整或自定义范围。自定义范围使用可展开文件树，选择父目录即覆盖当前及后续新增的全部后代；目录、SQLite、配置、文档、图片、代码和普通文件使用不同图标和色彩提示。单实例共享风险需要在此步骤确认。

范围不接受绝对路径、`..`、越界符号链接、特殊文件或 SQLite WAL、SHM、Journal 伴随文件。范围失效时的暂停与恢复规则见第 33 节。

### 19.4 第四步：计划设置

支持每小时、每天、每周和五段 Cron；每天和每周可选择本地执行时间，Cron 输入实时显示可读的执行含义，并在格式或字段不合法时说明原因。计划设置还包括时区、备份窗口、错过后不跑开关、通知和自动重试。

保留和重试说明常驻显示在高级选项中。V1 不提供清理、删除、回收站或自动保留删除；历史保留字段仅为控制库兼容数据，用户需要释放空间时直接在懒猫网盘侧管理文件。

### 19.5 确认和保存

确认区展示全部配置，并只提供“保存”。保存只创建或更新计划，不立即创建执行任务；计划列表中的“立即执行”用于单独发起一次执行。

## 20. 任务中心

任务中心展示逐任务历史记录，运行中与失败数量来自服务端概览接口。批次字段作为任务关联数据保留，不在任务中心单独展示“最近备份批次”组件。队列深度、队列位置、优先级、预计清空时间以及暂停/恢复操作不向用户展示；后端队列仍由执行引擎内部使用。


### 20.1 备份批次

字段：

- 批次 ID。
- 计划名称。
- 计划时间。
- 任务总数。
- 成功、警告、失败、跳过和运行数。
- 开始时间。
- 完成时间。
- 是否超过窗口。

应用、计划、批次、任务和快照详情统一使用居中弹窗。批次详情头部展示计划名、计划时间和状态；进度、成功和失败合为紧凑摘要，实例任务列表作为主要内容。点击某个任务后复用同一个弹窗切换到任务详情，提供返回上一层回到原批次详情，不叠加多个详情弹窗。

### 20.2 任务历史

筛选：

- 时间。
- 计划。
- 批次。
- 应用。
- 状态。
- 触发方式。
- 数据库类型。

字段：

- 计划时间。
- 开始和结束时间。
- 应用名称。
- `deploy_id`。
- 触发方式。
- 状态。
- 源大小。
- ZIP 大小。
- 文件数。
- 耗时。
- 输出目录。
- 失败任务的中英文可读失败原因，不显示稳定错误码。

### 20.4 任务详情

阶段：

1. 入队。
2. 获取租约和实例锁。
3. 身份与权限预检。
4. 解析 appvar。
5. 扫描和数据库检测。
6. 检查网盘空间。
7. 创建 SQLite 快照。
8. 创建 ZIP。
9. 计算 SHA-256。
10. 写入外部 manifest。
11. 更新索引和通知。

详情展示：

- 阶段时间线。
- 文件统计。
- SQLite 统计。
- ZIP 统计。
- 资源和吞吐。
- 重试记录。
- 结构化日志摘要。
- 输出路径。

任务详情弹窗以状态、尝试次数、完成时间和是否生成快照作为摘要，随后展示任务时间、计划要求的范围、范围失效原因（如有）和执行记录。任务从应用、批次或快照详情进入时，复用当前弹窗并提供返回上一层，不叠加新的详情遮罩。

## 21. 备份库

### 21.1 浏览方式

- 按应用：应用 → `deploy_id` → 时间。
- 按时间：日期 → 批次 → 应用。
- 按计划：计划 → 批次 → 快照。

不提供按用户浏览。

### 21.2 快照列表字段

- 快照时间。
- 应用名称和版本。
- 单实例或多实例。
- 文件数。
- 原始大小。
- ZIP 大小。
- SQLite 数量。
- 完整性状态。
- 最近校验时间。
- 保留状态。

备份库列表不单独展示 `deploy_id` 列；快照详情和按应用浏览路径继续保留该实例标识。

保留状态使用“保留中”或“已移入回收站”等快照生命周期文案，不使用“已启用/未启用”的开关文案。

### 21.3 快照详情

展示：

- 应用信息。
- `deploy_id`。
- 计划时间、捕获时间和完成时间。
- 网盘相对路径。
- `snapshot.zip` 大小和 SHA-256。
- 文件索引。
- SQLite 列表。
- 来源计划、批次和任务。

详情以只读方式展示上述信息和文件索引。范围声明、实际归档统计、SQLite 处理和创建时的完整性记录合并在“备份内容”区；文件索引默认显示前 30 项并标示总数。范围修订和跳过记录保留在 manifest 与服务端追溯数据中，不进入默认详情。打开详情不触发完整性校验，也不提供导入、导出或移入回收站操作。网盘目录显示 `MimiAppBakcup/` 下的完整路径；目录已被删除时明确显示目录状态，不能用文件索引读取失败替代目录状态。

### 21.4 文件索引

组件：

- 路径树。
- 文件表。
- 类型筛选。
- 名称搜索。
- 大小筛选。
- 修改时间。
- ZIP 内相对路径。
- 可选文件 SHA-256。

V1 不在线展示文件正文。

### 21.5 受限导出边界

导出能力仅保留为服务端维护边界；当前 V1 快照详情不提供用户入口。若后续开放入口，只能将 `snapshot.zip` 解压到当前用户网盘的独立目录，并且：

- 不写入目标应用。
- 不覆盖已有目录。
- 预估空间。
- 校验文件数量。
- 防止 ZIP 路径穿越。

## 22. 存储页

### 22.1 存储概览

- 当前用户 UID。
- 固定备份根目录。
- 已用空间。
- 可用空间。
- 快照数量。
- ZIP 总大小。
- 临时任务数量。
- 最近写入和校验时间。

容量条使用 `已用 / (已用 + 可用)`。占用为零时显示 `0%`；非零但小于 0.1% 时显示 `<0.1%`，并以最小可见填充表示已用空间，不能把已有归档误呈现为空。页面以容量主卡承载已用、可用和扫描操作，快照数、最近校验、校验异常和部分写入使用紧凑摘要，避免以大面积空白色块分散注意力。

### 22.2 固定目录结构

```text
MimiAppBakcup/
└── <deploy_id>/
    └── <scheduled_at>/
        ├── snapshot.zip
        └── manifest.json
```

每个 `deploy_id` 下的目录时间使用当前用户设置页选择的 IANA 时区，并把时区标识写入目录名：

```text
yyyy-MM-dd_HH-mm-ss.SSS_<timezone>
```

例如 `2026-08-28_09-18-01.653_Asia-Shanghai`。后端快照元数据仍使用带偏移的 RFC 3339 时间，页面按同一设置时区显示。

同一应用实例在同一毫秒发生冲突时，在时间目录后追加批次短 ID。

应用名称、`appid`、版本、实例模式和用户信息写入 `manifest.json`，不加入物理目录层级。

### 22.3 临时目录

任务写入期间使用：

```text
MimiAppBakcup/_partial/<job_id>/
```

失败任务不进入备份库。临时目录状态只展示，不由产品自动清理。

### 22.4 存储维护

- 立即扫描。
- 重建快照索引。
- 查看占用最大的应用。
- 校验 ZIP 与 manifest 对应关系。

## 23. ZIP 快照格式

### 23.1 最终文件

每个快照目录只包含：

```text
snapshot.zip
manifest.json
```

### 23.2 ZIP 内容

```text
snapshot.zip
├── appvar/
│   └── 应用数据相对目录
└── _snapshot/
    ├── manifest.json
    ├── file-index.jsonl
    └── warnings.json
```

ZIP 内外各保存一份 manifest。外部 manifest 供备份库快速读取；ZIP 内 manifest 使压缩包单独移动后仍可识别。

### 23.3 完成判定

外部 `manifest.json` 最后写入，且必须包含：

```text
status = completed
archive_name = snapshot.zip
archive_sha256
archive_size
```

备份库只接受：

- 外部 manifest 存在。
- `status` 为 `completed`。
- `snapshot.zip` 存在。
- ZIP SHA-256 与 manifest 一致。

不再使用独立 `COMPLETED` 文件。

## 24. 告警页

### 24.1 告警级别

- 信息。
- 警告。
- 严重。
- 紧急。

### 24.2 告警类型

- OIDC 登录失败。
- 已登录会话 UID 与当前入口账户 UID 不一致。
- 必需权限缺失。
- appvar 投影不可用。
- 应用目录查询失败。
- 单实例共享数据计划提醒。
- 检测到不支持数据库。
- SQLite 在线快照失败。
- 普通文件持续变化。
- 网盘不可写。
- 空间不足。
- ZIP 创建失败。
- ZIP 校验失败。
- 批次超出窗口。
- 连续任务失败。
- 临时目录异常。
- 控制数据库异常。
- 多实例应用未能自动运行导致漏跑。

### 24.3 操作

- 查看关联应用、任务、批次或快照。
- 立即重试。
- 重新检测。
- 查看修复建议。
- 标记已读。
- 确认已处理。
- 临时静默。

## 25. 设置页

设置页采用目录式双栏。左侧提供“个人”“备份偏好”“维护与记录”分类入口，右侧直接展示当前选中条目的内容；不再在内容顶部重复显示设置菜单。移动端将左侧目录压缩为可横向滚动的菜单，保证所有分类入口仍可见。可编辑参数在修改后立即保存并提示结果，维护与记录页面不显示保存按钮。

### 25.1 账户与登录

- 当前 OIDC 用户。
- UID。
- 角色。
- 会话到期时间。
- 重新登录。
- 退出登录。
- 身份一致性状态。

### 25.2 外观

- 页面语言。
- 默认时区。

### 25.3 备份偏好

- 默认备份时间和时区。
- 错过后不跑开关。
- 自动重试次数和退避间隔。
- 保留兼容字段及其不触发自动删除的说明。

### 25.4 通知

- 懒猫平台消息使用可选 `user.notify` 权限；权限不可用时保留站内告警。
- 浏览器通知由用户在当前浏览器显式授权；授权后，已打开且保持运行的应用页面可显示浏览器级系统横幅。关闭页面或退出浏览器后，本版不提供后台推送。
- 首次失败，向懒猫平台消息和已授权浏览器发送任务终态通知。
- 连续失败升级。
- 空间不足。
- 超出窗口。
- 校验失败。
- 成功通知，每次成功完成或带警告完成时向懒猫平台消息和已授权浏览器发送任务终态通知。
- 每日摘要。

### 25.5 维护与记录

- 当前用户网盘根目录，只读展示。
- 审计记录。
- 权限与环境状态。

产品不提供任何清理或删除参数，也不在设置页开放未接入执行引擎的性能或备份引擎参数。

不提供用户 UID 选择器和任意宿主机路径输入。

### 25.6 权限与环境

- `appvar.other.read`。
- `document.write`。
- `user.notify`。
- 兼容权限状态。
- appvar 投影状态。
- 当前用户应用数量。
- LzcOS 版本。
- 应用版本。
- 后台运行状态。
- 最近检测时间。

页面不显示其他用户，不提供跨用户操作。

## 26. 专用状态

### 26.1 无应用数据

文案：

> 该应用当前没有需要备份的运行数据。

操作：重新检测、返回。

### 26.2 数据库不支持

展示：

- 数据库类型。
- 相对路径。
- V1 不支持原因。
- 后续适配计划。

操作：查看详情、重新检测、返回。

不提供强制备份按钮。

### 26.3 单实例共享风险

文案：

> 该应用使用单实例运行，应用内部可能保存共享数据。当前备份范围以懒猫平台向本账号开放的数据目录为准。

操作：

- 我已了解并继续。
- 取消。
- 查看数据范围。

### 26.4 权限或投影异常

展示：

- 缺失权限。
- 投影状态。
- 追踪 ID。
- 重新检测。
- 设置入口。

LightOS 隔离助手数据使用同一受限状态。页面展示“LightOS 助手数据暂不支持备份”，不提供重新检测作为修复动作。

## 27. 业务流程

### 27.1 应用发现

1. OIDC 登录完成。
2. 后端确认当前 `tenant_uid`。
3. 查询全部已安装应用目录。
4. 过滤备份应用自身。
5. 建立应用和 `deploy_id` 记录。
6. 解析目录中可用的 appvar。
7. 执行轻量能力检测。
8. 前端分页展示。

### 27.2 手动备份

1. 用户选择应用。
2. 系统执行完整预检。
3. 单实例时显示共享数据风险提示。
4. 创建批次和任务。
5. 任务进入有界工作池。
6. 创建 SQLite 快照。
7. 普通文件和 SQLite 写入 ZIP。
8. 计算 ZIP SHA-256。
9. 写入 ZIP 内 manifest。
10. 关闭 ZIP。
11. 写入外部 manifest。
12. 快照进入备份库。
13. 发送结果通知。

### 27.3 定时备份

1. 调度器读取到期计划。
2. 重新查询全部已安装应用目录。
3. 展开目标并排除不可备份应用。
4. 创建批次和任务。
5. 所有任务保存同一个 `scheduled_at`。
6. 工作池受控执行。
7. 超过窗口时按计划继续、跳过或告警。
8. 写入批次完成状态，不删除任何旧快照。

### 27.4 SQLite

1. 识别标准 SQLite 文件头。
2. 排除原始 DB、WAL、SHM 和 Journal 的普通复制。
3. 在短生命周期临时目录创建 SQLite 一致性副本。
4. 执行 `quick_check`。
5. 将副本写入 ZIP 中原数据库对应的相对路径。
6. 清理临时文件。
7. 失败时整项任务失败。

### 27.5 不支持数据库

1. 扫描发现数据库特征。
2. 任务在创建 ZIP 前终止。
3. 应用状态更新。
4. 产生告警。
5. 不生成成功快照。

### 27.6 应用重启

1. 打开控制数据库。
2. 恢复调度器。
3. 回收过期租约。
4. 对账 `_partial`。
5. 恢复可重试任务。
6. 按补跑窗口处理漏跑计划。
7. 不覆盖历史成功快照。

## 28. 状态定义

### 28.1 可备份性

- `BACKUPABLE`
- `BACKUPABLE_SHARED_WARNING`
- `NO_DATA`
- `UNSUPPORTED_DATABASE`
- `UNKNOWN_DATABASE`
- `SYSTEM_UNSUPPORTED`
- `SOURCE_NOT_READY`
- `SOURCE_PERMISSION_DENIED`
- `SOURCE_INSTANCE_NOT_FOUND`
- `SOURCE_OWNER_MISMATCH`
- `PROBE_FAILED`

`PROBE_FAILED` 只表示未分类的真实探测异常。应用数据源未出现在已授权 appvar 投影中、投影不可见、读取被拒绝，或扫描超过 100,000 个目录项时使用 `SYSTEM_UNSUPPORTED`；后者记录 `SOURCE_ENTRY_LIMIT_EXCEEDED`。页面显示可读状态，不展示内部原因码。

### 28.2 任务

- 等待中。
- 已租约。
- 预检中。
- 扫描中。
- SQLite 快照中。
- ZIP 创建中。
- 校验中。
- 提交中。
- 成功。
- 成功但有警告。
- 失败。
- 已取消。
- 已超时。
- 已跳过。
- 异常中止。

### 28.3 快照

- 写入中。
- 已完成未校验。
- 校验通过。
- 校验警告。
- 校验失败。

## 29. 非功能要求

### 29.1 安全

- OIDC 登录为必需条件。
- 所有接口校验当前用户身份。
- 不使用 `other_uid`。
- 不接受用户 UID、owner UID 和源绝对路径作为授权参数。
- 源数据只读。
- 不跟随越过源根的符号链接。
- 跳过特殊设备和 Socket。
- 文件正文不进入日志和诊断包。
- ZIP 解压时防止路径穿越。
- 应用目录对管理员和普通用户使用同一全量范围；角色不改变计划、任务、快照和网盘的当前会话隔离。

### 29.2 性能

- 列表使用服务端分页和虚拟滚动。
- 单个用户实例支持至少 10,000 条应用实例记录。
- 单批次可以持久化至少 5,000 个任务记录。
- 任务创建不启动同等数量 goroutine。
- ZIP 使用有界缓冲。
- 大文件流式写入。
- SQLite 并发独立限流。
- 前端在 100,000 条任务历史下可以分页浏览。
- 前端在一个会话内只维护一条 SSE 订阅；事件合并刷新，断线后退避重连，避免重复订阅和并发整页刷新。

### 29.3 可靠性

- 计划、批次、任务和租约持久化。
- 任务具有幂等键。
- 失败不删除旧快照。
- 外部 manifest 最后写入。
- ZIP 与 manifest 校验一致。
- SQLite 失败不降级。
- 后端重启后可以恢复未完成的任务执行状态。

### 29.4 国际化

- 中文和英文覆盖所有页面、状态、错误和通知。
- 后端返回稳定错误码与参数。
- 任务中心、任务详情和失败计划卡片将稳定错误码转换为当前语言的可读原因；未知错误码显示通用失败说明，不回显内部代码。
- 日期、容量、速度和百分比按语言格式化；日期使用当前设置时区并显示时区名称。
- 切换语言不丢失筛选和表单草稿。

### 29.5 可用性

- 状态同时使用图标和文字。
- 表单有明确错误说明。
- 主要操作支持键盘。
- 移动端支持查看概览、任务、快照和告警。
- 复杂计划编辑以桌面端为主。

## 30. V1 验收标准

### 30.1 登录和隔离

- 未登录时显示懒猫 OIDC 登录页；只有用户点击登录按钮才进入授权，回调成功后进入首页。
- 已登录业务请求中的 `session.gateway_uid` 与 `X-HC-User-ID` 一致。
- 普通用户和管理员都能看到全部已安装应用；任务、快照和网盘仍按当前登录用户隔离。
- 不存在用户切换功能。
- 备份结果只写入当前用户网盘。

### 30.2 数据获取

- 当前已跑通的应用目录、appvar 投影、探测和手动备份链路继续工作。
- 支持平台返回的全部已安装单实例和多实例应用。
- 单实例显示共享风险。
- 能备份普通文件和目录。
- 能创建 SQLite 一致性副本。
- 已知不支持数据库被阻断。
- 无数据应用不创建空快照。

### 30.3 ZIP 和目录

- 归档格式为 ZIP。
- 最终路径为：

```text
MimiAppBakcup/<deploy_id>/<时间>/snapshot.zip
MimiAppBakcup/<deploy_id>/<时间>/manifest.json
```

- ZIP 内含 `appvar/` 和内部 manifest。
- 外部 manifest 最后写入。
- ZIP SHA-256 与 manifest 一致。
- 失败任务不进入备份库。

### 30.4 页面

- 八个主菜单和全部详情页可用。
- 登录、会话过期和身份错误状态可用。
- 应用列表不包含其他用户。
- 单实例、多实例、无数据、数据库阻断和权限异常均有明确状态。
- POC 探测页面不出现在正式导航。

### 30.5 调度

- 支持立即、每天、每周和 Cron。
- 支持批量目标。
- 同一个 `deploy_id` 不会并发运行两个备份。
- 后台任务受资源限制。
- 重启后可以补跑。

### 30.6 中英文

- 中文和英文功能一致。
- OIDC 登录错误、权限、任务和通知均完成翻译。

## 31. 后续功能

- 面向管理员的集中式跨用户控制库、计划、任务、快照和网盘管理，需独立权限、授权和审计。
- 一键恢复组件，独立申请写入权限。
- MySQL、PostgreSQL、MongoDB、Redis 适配器。
- 应用 `prepare/export/complete` 备份协议。
- Notus 专用索引 flush 适配器。
- S3、WebDAV、SFTP、NAS 和异地微服。
- 客户端加密与密钥导出。
- 增量 ZIP、内容寻址和去重。
- 自动恢复演练。
- 懒猫网盘、相册和 LightOS 官方接口适配。

## 32. 参考资料

- [懒猫 OIDC](https://developer.lazycat.cloud/advanced-oidc.html)
- [懒猫 HTTP Headers](https://developer.lazycat.cloud/http-request-headers.html)
- [懒猫多实例](https://developer.lazycat.cloud/advanced-multi-instance.html)
- [懒猫开发者环境变量](https://developer.lazycat.cloud/advanced-envs.html)
- [package.yml 权限规范](https://developer.lazycat.cloud/spec/package.html)
- [lzc-manifest.yml 规范](https://developer.lazycat.cloud/spec/manifest.html)
- [文件访问](https://developer.lazycat.cloud/advanced-file.html)
- [LightOS](https://developer.lazycat.cloud/advanced-lightos.html)
- [当前项目仓库](https://github.com/dnwwdwd/lazycat-app-snapshot)
- [SQLite Online Backup API](https://sqlite.org/backup.html)
- [Go archive/zip](https://pkg.go.dev/archive/zip)

## 33. 选择性备份范围

### 33.1 范围和完整性

计划的每个应用目标独立选择完整或自定义范围。完整备份包含当前应用数据根下全部可支持的普通文件和标准 SQLite。符号链接、特殊文件、SQLite WAL/SHM/journal 伴随文件继续按既有安全规则跳过，完整标记不变；跳过记录保留在 manifest 和服务端，不占用默认详情。

自定义范围支持目录和单文件。目录包含其当前内容，内部文件的日常增删不暂停计划。范围选择器以可展开文件树显示层级；选中父目录即选中全部子目录和文件。既有核心范围记录继续保留可读、可执行的兼容行为，但不再向新建或编辑计划提供，因为应用核心数据无法可靠判定。

### 33.2 失效与恢复

计划每次到期和用户手动运行计划时，会先校验所有目标。已选目录、单文件或源根在原相对路径不存在、类型变化或不可读取时，暂停整项多应用计划，清空下一次运行时间，记录应用、路径、预期类型和检测时间。界面统一提示“目录或文件已删除或移动”。系统不搜索新路径，不自动迁移选择，也不自动恢复计划。

重新选择范围并保存后，目标范围修订递增，未启动的旧修订任务取消，暂停原因清除并重新计算计划时间。运行中的旧修订任务可安全收尾。立即备份在本版保持完整备份。

### 33.3 可见信息

计划编辑器采用四步向导：备份模式、添加应用、配置范围和计划设置。计划列表和告警显示暂停原因及“重新选择范围”入口。计划列表用“每天 02:00”“每周一 02:00”或 Cron 的可读解释呈现频率，操作收敛为查看、立即执行、暂停/启用与编辑图标。任务详情分开呈现计划要求的范围和本次实际归档；快照详情显示完整或部分标记、声明目录/文件、归档统计、SQLite 处理和文件索引。范围修订和跳过记录保留用于追溯，不进入默认详情。旧记录显示“旧版完整备份”，不伪造历史精确路径。

新建或编辑计划的确认区只提供“保存”。保存只创建或更新计划，不立即创建执行任务；计划列表保留独立的“立即执行”操作。

## 34. 变更记录

| 日期 | 文档版本 | 变更 |
| --- | --- | --- |
| 2026-09-01 | V1 | 保护概览图表选中状态放大右侧对应的图例文字和数字，并移除额外的悬停提示文案。 |
| 2026-09-01 | V1 | 保护概览改用 overview 真实状态数量绘制可交互图表；应用和备份库列表移除独立部署实例列，`deploy_id` 保留在筛选、详情和关联数据中；数据库特征标签按内容自适应宽度。 |
| 2026-09-01 | V1 | 任务中心表格和任务详情将所有任务稳定错误码显示为中英文可读原因，不回显内部代码；计划最近批次失败时按该批次任务汇总并显示失败原因。 |
| 2026-09-01 | V1 | 任务成功和最终失败通知扩展为双通道：后端继续发送懒猫平台消息；用户显式授权后，已打开页面通过实时任务事件显示浏览器级通知。首次加载不重放历史任务弹窗，关闭页面或退出浏览器时不提供后台推送。 |
| 2026-09-01 | V1 | 目录扫描达到 100,000 个目录项上限时，使用 `SYSTEM_UNSUPPORTED/SOURCE_ENTRY_LIMIT_EXCEEDED`，不再落入 `PROBE_FAILED` 兜底；规则不依赖应用 ID、名称或商店标签。 |
| 2026-08-31 | V1 | 快照网盘目录改为 `MimiAppBakcup/<deploy_id>/<时间>/`；同一实例的时间目录冲突时追加批次短 ID，详情和一致性校验沿用快照记录中的最终相对路径。 |
| 2026-08-31 | V1 | 明确管理员与普通用户共同使用全部已安装应用；应用 `owner` 不再作为目录过滤条件，计划、任务、快照和网盘仍按当前登录用户隔离。 |
| 2026-08-31 | V1 | 数据源未出现在已授权 appvar 投影中、投影不可见或读取被拒绝时显示“系统或权限受限”，不再归类为“需要重新检测”；未分类探测错误与真实权限拒绝分开呈现。 |
| 2026-08-31 | V1 | 首屏数据就绪后不再自动显示右上角 toast；toast 只提示用户主动操作的结果，不显示 `zh-CN` 或 `en-US` 语言代码。 |
| 2026-08-31 | V1 | 应用页数据概览分行展示文件与 SQLite 数量，SQLite 为零时隐藏；备份库使用应用实例真实图标；任务中心移除“最近备份批次”组件。 |
| 2026-08-31 | V1 | 任务中心收敛为备份批次与任务历史，移除无用户侧 API 支持的队列概况、排队筛选和暂停/恢复操作；队列仅保留为后端内部执行机制。 |
| 2026-08-31 | V1 | 首次进入应用时增加应用目录全局 loading；应用数据首次准备完成后显示一次中英文站内提示，并避免加载期间误显示空列表。 |
| 2026-08-31 | V1 | 侧边栏页面徽标改为显示当前租户的全量数量，不再显示当前列表分页数量。 |
| 2026-08-31 | V1 | 应用页提交重新扫描后显示中英文站内提示，明确刷新请求已提交并正在同步。 |
| 2026-08-31 | V1 | 应用列表将任务和快照入口收纳到详情弹窗；单应用创建计划固定当前实例；立即备份按钮统一命名；设置目录只保留侧栏入口并适配移动端；数据库标签明确区分有效 SQLite 与未知数据库。 |
| 2026-08-31 | V1 | 用户确认目标 LPK 包标识为 `cloud.lazycat.app.mimi-app-backup`；应用子域名、多实例、OIDC 回调路径和业务权限保持不变。 |
| 2026-08-31 | V1 | 按用户要求将 LPK 包标识恢复为项目初始使用的 `cloud.lazycat.app.backup`；应用子域名、多实例、OIDC 回调路径和业务权限保持不变。 |
| 2026-08-30 | V1 | LPK 包标识更新为 `cloud.lazycat.app.mimi-app-backup`，中文包说明更新为“自动备份懒猫应用数据”。 |
| 2026-08-30 | V1 | 正式前端以 `designs/web.tsx` 作为唯一视觉与页面结构来源，八个页面改用当前账号 REST API 和单条 SSE 数据控制层；列表改为服务端游标分页，应用、计划目标、任务和快照均以 `deployId` 关联。移除首次使用向导、总队列暂停/恢复、演示指标及无接口详情字段；单实例立即备份展示风险后直接入队。 |
| 2026-08-30 | V1 | 对齐既有需求记录：侧栏不再提供语言切换；应用详情移除重复的立即备份和重新检测；计划编辑明确为备份模式、添加应用、配置范围、计划设置四步；快照默认详情隐藏范围修订和跳过记录；设置页明确为目录式双栏并即时保存。导出服务边界保留，V1 快照详情不提供导出入口。 |
| 2026-08-29 | V1 | 任务详情中的“尝试记录”统一改称“执行记录”，仅调整界面文案，不改变任务尝试数据。 |
| 2026-08-29 | V1 | 存储页使用精确的小占用文案和最小可见进度，不把非零 ZIP 占用显示为 `0%`；容量、快照和维护状态重排为紧凑布局。 |
| 2026-08-29 | V1 | 应用、计划、批次、任务和快照详情统一使用居中弹窗，并支持同一弹窗内的任务/快照往返；计划、任务中心按当前操作和历史记录分层；快照详情合并范围、归档和完整性信息，文件索引默认精简展示。 |
| 2026-08-29 | V1 | 计划编辑确认区只保留黄色“保存”按钮；保存不立即入队，计划列表提供独立的立即执行操作。 |
| 2026-08-29 | V1 | 快照默认详情移除范围修订和跳过项；计划列表以可读周期或 Cron 含义显示执行频率，并将查看、执行、暂停/启用和编辑收敛为图标操作。 |
| 2026-08-29 | V1 | 选择性范围的创建与编辑收敛为完整和自定义两种模式；自定义范围改为父目录覆盖后代的可展开文件树。历史核心范围仅保留兼容读取和执行。 |
| 2026-08-29 | V1 | 计划目标支持完整、Notus 核心与自定义目录/单文件范围；范围失效暂停整项计划，任务和快照展示声明范围、实际归档与失效原因；立即备份仍为完整备份。 |
| 2026-08-28 | V1 | 备份库和任务中心详情改为分层只读工作区；快照详情不触发完整性校验、不提供导入/导出/回收站操作，显示完整网盘路径及目录删除状态；设置中的错过后补跑改为开关；统一提升表格表头样式和字号。 |
| 2026-08-28 | V1 | 网盘备份根目录由 `LazycatAppBackup` 更名为 `MimiAppBakcup`。升级后服务只使用新目录，不迁移、合并或修改旧目录。 |
| 2026-08-28 | V1 | 所有主页面移除标题上方的环境与 tenant 标识；侧栏不再显示语言选择，页面语言仅在设置页外观条目中修改。 |
| 2026-08-28 | V1 | 设置页改为目录式双栏：左侧分类入口，右侧按条目进入个人、外观、通知、备份调度和审计记录；参数修改后立即自动保存，维护与记录不显示保存按钮。 |
| 2026-08-28 | V1 | 计划编辑改为应用搜索、图标化批量目标和分层频率选择；备份库详情、任务中心与存储维护改为以快照、批次进度和容量维护为中心的工作区布局，产品不提供清理和删除入口。 |
| 2026-08-28 | V1 | 顶部菜单栏移除，账户操作收敛到侧栏；当时语言切换也移入侧栏，后续同日调整为仅在设置页外观条目中修改。立即备份先显示确认弹窗，不可备份实例保留禁用图标；快照目录改用设置时区下的可读时间字符串并包含时区标识；状态标签使用图标和文字；设置页重排为账户、时间与调度、执行策略、保留、提醒和审计分区。 |
| 2026-08-27 | V1 | 参考 `agent-desk` 的会话模型修正身份判断：OIDC `preferred_username` 只保存为 profile UID；回调保存 `X-HC-User-ID` 为 `session.gateway_uid`；`IDENTITY_MISMATCH` 仅表示该字段与当前请求 Header 不一致。 |
| 2026-08-27 | V1 | 正式子域名使用 `mimi-app-backup`，POC 使用 `mimi-app-backup-poc`；已安装实例需通过升级或重装新的 LPK 才会获得新的平台分配域名。 |
| 2026-08-27 | V1 | 产品更名为“咪咪应用备份（Mimi App Backup）”，LPK 包标识改为 `mimi-app-backup`，前端菜单与登录页同步更新；本地 LPK 产物为 `mimi-app-backup-0.1.0.lpk`。 |
| 2026-08-27 | V1 | 登录入口采用显式 OIDC 登录页：用户点击后才创建授权事务，回调成功后进入首页；网关 UID 不匹配会清除旧会话并回到登录页。 |
| 2026-08-28 | V1 | 备份计划仅支持用户显式选择当前可备份应用，可搜索、查看图标并一次添加多个目标；不再提供未来新增应用自动加入计划的规则。 |
| 2026-08-27 | V1 | 阶段 5 完成本地实现：首次使用检查、概览、完整计划编辑、任务与批次详情、备份库和文件索引、存储维护、站内告警、当前用户设置、审计、限时 SSE 与中英文文案全部改用正式 API。外部 `user.notify`、OIDC 回调、网关 UID 会话绑定、A/B 用户隔离、网盘写入和后台补跑仍待真实平台验收；该阶段记录不包含 LPK 打包。 |
| 2026-08-27 | V1 | 完成阶段 4 本地实现：计划、批次、任务队列、租约恢复、重试和补跑接入正式 API；备份库支持文件索引、完整校验、导出和存储扫描，不提供回收站、删除或自动清理。完整计划向导、告警、SSE、设置和中英文完整界面仍待后续阶段。 |
| 2026-08-27 | V1 | 完成当前用户单实例手动备份的本地实现：应用页可发起备份并确认共享风险，备份库显示真实快照和快速校验；真实平台网盘写入及 A/B 隔离仍待验收。 |
| 2026-08-27 | V1 | 重写用户模型、OIDC、单/多实例支持、管理员边界、ZIP 格式和网盘目录 |
