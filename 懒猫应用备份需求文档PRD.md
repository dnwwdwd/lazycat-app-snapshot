# 懒猫应用备份 PRD

> 文档版本：V1  
> 产品名称：懒猫应用备份（Lazycat App Backup）  
> 产品形态：安装在懒猫微服中的管理员 Web 应用  
> 目标平台：懒猫微服 LPK V2  
> 支持语言：简体中文（zh-CN）、English（en-US）  
> 最后更新：2026-08-24  
> 说明：本文档版本固定为 V1，后续修改通过“最后更新”和变更记录体现，不再递增文档子版本。

## 1. 产品背景

懒猫微服应用通常将数据库、索引、配置、附件元数据和运行状态保存在应用实例自己的 `appvar` 中。一旦应用升级、程序 Bug、误删除、异常重建或磁盘故障导致这些文件丢失，用户可能需要重新初始化应用，或花费较长时间重建索引。

懒猫应用备份用于统一发现微服中的应用及部署实例，根据管理员配置执行手动或定时备份，并将备份结果直接写入懒猫网盘。产品按懒猫应用生态 4000+ 应用的规模进行架构设计，要求控制面能够管理上万条应用实例记录，并能在同一计划时间点快速创建、排队和处理成百上千个实例备份任务。

“同一时间点备份”在产品中表示：所有命中的应用实例在同一个计划时间形成一个备份批次，并在允许的备份窗口内由资源受控的工作池执行。系统不会无上限地同时读取所有实例，否则可能压垮微服磁盘。每个实例均记录计划时间、实际开始时间、实际快照时间和完成时间。

## 2. 产品定位

本产品 V1 是一个“应用文件与 SQLite 备份管理器”，不是通用数据库备份平台，也不是整机镜像工具。

V1 核心能力：

1. 读取其他应用实例可访问的 `appvar` 数据。
2. 备份普通文件、目录以及标准 SQLite 数据库。
3. 多实例应用按 `deploy_id` 和所属用户分别管理。
4. 支持单实例、应用级动态实例、批量应用的手动与定时备份。
5. 将备份归档直接写入懒猫网盘，不长期存放在备份应用容器中。
6. 对无数据、权限受限、系统应用、特殊存储应用和不支持数据库给出明确状态。
7. 支撑大批量任务的排队、并发控制、进度展示、失败重试和告警。

## 3. 产品目标

### 3.1 V1 目标

1. 管理员可以查看微服中所有可发现的应用及部署实例。
2. 每个应用实例以 `appid + instance_key + owner_uid` 作为备份目标身份；多实例的 `instance_key` 使用平台 `deploy_id`。
3. 管理员可以选择单个实例、某应用的全部实例或多个应用创建备份计划。
4. 支持立即备份、定时备份和错过任务后的补跑。
5. 普通文件在应用运行期间进行受控读取和归档。
6. 标准 SQLite 数据库在应用运行期间通过 SQLite Online Backup API 创建一致性副本，不直接复制活动数据库文件。
7. 检测到 MySQL、PostgreSQL、MongoDB、Redis 或其他已知不支持数据库时，阻止该实例备份。
8. 备份数据按应用名称、用户、`deploy_id` 和备份时间形成目录树写入懒猫网盘。
9. 管理端可以查看批次、队列、实例任务、快照、文件索引、校验结果和告警。
10. 系统能够管理至少 10,000 条应用实例目录记录，并能在一个计划时间点创建至少 5,000 个实例任务。
11. 中文和英文功能完全一致。

### 3.2 成功标准

- 用户能够为 Notus 创建每天自动备份计划，并在网盘中找到对应时间目录。
- Notus 的普通数据文件和标准 SQLite 文件能够被完整备份。
- 多实例应用的不同 `deploy_id` 不会混入同一份备份。
- 检测到中型数据库或未知文件数据库时不会生成具有误导性的“成功备份”。
- 纯前端应用或空 `appvar` 不产生空归档，也不计入未保护风险。
- 同一时间点命中大量应用时，任务能够快速进入持久化队列，并按照资源策略稳定执行，不因创建大量 goroutine 或同时打开大量文件而失控。
- 任务中断后可以恢复队列状态，已经完成的备份不会因后续任务失败而丢失。

## 4. 非目标与安全边界

V1 不实现以下能力：

- 不申请 `appvar.other.write`，不直接写回或覆盖其他应用的 `appvar`。
- 不提供一键恢复到目标应用。
- 不自动停止、启动或重启其他应用。
- 不备份 MySQL、PostgreSQL、MongoDB、Redis、Elasticsearch、ClickHouse 等服务型数据库。
- 不正式支持 DuckDB、LMDB、RocksDB、LevelDB、Pebble、bbolt、BadgerDB、H2、HSQLDB、Derby 等其他文件或目录型数据库。
- 不备份 LightOS 实例系统盘。
- 不承诺备份懒猫网盘、懒猫相册等系统级或特殊存储应用的核心数据。
- 不读取其他应用的应用文稿、媒体目录、RemoteFS 或外部 NAS。
- 不做整机、容器镜像、块设备或操作系统配置备份。
- 不通过 `compose.override`、宿主机目录挂载、块设备权限等方式绕过系统安全边界。
- 不把备份归档长期存放在 `/lzcapp/var` 或容器根文件系统。
- 不保证普通文件在应用持续写入期间具备跨文件事务一致性；V1 只对标准 SQLite 提供数据库一致性快照。

## 5. 用户角色与权限

### 5.1 微服管理员

产品仅面向微服管理员。管理员可以：

- 查看应用、用户和部署实例。
- 查看应用是否可备份以及被阻断的原因。
- 创建、编辑、启用、暂停和删除备份计划。
- 批量选择应用或实例执行立即备份。
- 查看任务队列、批次、单实例任务和快照。
- 校验备份归档并导出恢复副本到网盘目录。
- 设置调度并发、备份窗口、保留策略和通知。
- 查看环境诊断和脱敏日志。

### 5.2 权限原则

V1 所需权限：

- `appvar.other.read`：读取其他应用实例的 `appvar`。
- `document.write`：将用户可理解的备份归档写入当前用户的懒猫网盘公共文稿目录。
- `user.notify`：可选，用于发送系统通知。

被备份应用无需声明或开启任何权限。权限由备份应用申请并获得系统授权。

V1 不申请：

- `appvar.other.write`
- `compose.override`
- `device.block`
- `lightos.manage`
- `document.read`
- `media.read`

## 6. 数据支持矩阵

| 数据类型 | V1 行为 | 一致性 | 是否允许创建计划 |
| --- | --- | --- | --- |
| 普通文件和目录 | 读取并归档 | 文件级、尽力一致 | 是 |
| JSON、YAML、TOML、CSV、Markdown、附件、图片、模型文件 | 按普通文件归档 | 文件级、尽力一致 | 是 |
| 向量索引、全文索引、自定义二进制目录 | 按普通文件归档并提示可能存在运行时写入 | 文件级、尽力一致 | 是 |
| 标准 SQLite 3 | 使用 Online Backup API 创建临时一致性副本后归档 | SQLite 快照一致 | 是 |
| SQLite WAL/SHM/Journal 辅助文件 | 不单独复制，由 SQLite 快照覆盖 | SQLite 快照一致 | 是 |
| SQLCipher 或加密 SQLite | 标记为不支持或待确认 | 不保证 | 否 |
| MySQL | 检测并阻断 | 不支持 | 否 |
| PostgreSQL | 检测并阻断 | 不支持 | 否 |
| MongoDB | 检测并阻断 | 不支持 | 否 |
| Redis RDB/AOF | 检测并阻断 | 不支持 | 否 |
| DuckDB、LMDB、RocksDB、LevelDB、bbolt、BadgerDB 等 | 检测到已知特征时阻断 | 不支持 | 否 |
| Unix Socket、命名管道、设备文件、PID/Lock 临时文件 | 跳过并记录 | 不适用 | 不单独阻断 |
| 空 `appvar` 或只有可忽略临时文件 | 显示“无应用数据” | 不适用 | 否 |
| 数据位于应用文稿、媒体、RemoteFS、LightOS 或系统存储 | 显示“数据不在支持范围” | 不适用 | 否 |

### 6.1 未识别数据库文件

当文件扩展名、目录结构或二进制特征强烈表明其为数据库，但无法确认是标准 SQLite 时，实例状态为“检测到未知数据库”，V1 阻止备份。用户不能通过“仍然备份”绕过该阻断。

### 6.2 普通文件写入风险

普通文件在复制前后会检查大小、修改时间和状态是否变化。文件持续变化时进行有限次数重试；仍不稳定时，严格模式下本次快照失败，避免把不完整备份显示为成功。

## 7. 核心业务对象

### 7.1 应用包

表示一个安装包或应用产品，核心字段包括应用名称、`appid`、版本、图标、是否内置和是否多实例。

### 7.2 应用实例

表示实际可运行和拥有独立 `appvar` 的部署实例。唯一身份为：

```text
appid + instance_key + owner_uid
```

- 多实例应用：每个用户对应一个或多个独立 `deploy_id`。
- 单实例应用：优先使用平台 `deploy_id`；缺失时使用稳定 shared instance key，所属用户在产品中显示为“共享实例”。

### 7.3 备份计划

描述目标选择规则、执行时间、备份窗口、并发策略、保留策略和通知规则。计划可以绑定：

- 一个具体实例。
- 一个应用当前的全部实例。
- 一个应用当前及未来新增的全部实例。
- 多个应用或多个实例。

### 7.4 备份批次

同一个计划时间点展开出的任务集合。一个批次可以包含一个或数千个实例任务。

### 7.5 实例任务

批次内针对一个 `deploy_id` 的具体执行单元。每个实例任务拥有独立状态、实际执行时间、快照时间、错误、重试次数和输出目录。

### 7.6 快照

一次实例任务产生的可校验备份结果。快照分为：

- 完整快照：包含完整压缩归档。
- 无变化引用快照：源数据指纹未变化，仅记录时间和引用的上一份完整快照。

系统会按配置周期强制创建完整快照，避免长期只依赖元数据指纹。

## 8. 信息架构与主菜单

### 8.1 主导航菜单

| 菜单名称 | 英文名称 | 路由 | 页面目标 |
| --- | --- | --- | --- |
| 概览 | Overview | `/overview` | 查看整体保护状态、任务、风险和容量 |
| 应用与实例 | Applications & Instances | `/applications` | 管理应用、实例、能力检测和批量备份 |
| 备份计划 | Backup Plans | `/plans` | 创建和管理手动、定时及动态实例计划 |
| 任务中心 | Task Center | `/tasks` | 查看运行队列、备份批次和实例任务历史 |
| 备份库 | Backup Library | `/backups` | 按应用、用户、实例和时间浏览快照 |
| 存储 | Storage | `/storage` | 管理懒猫网盘目标、容量、临时目录和清理 |
| 告警 | Alerts | `/alerts` | 查看并处理权限、任务、数据库和存储告警 |
| 设置 | Settings | `/settings` | 配置调度、并发、通知、语言和诊断 |

### 8.2 非主菜单页面

- 首次启动向导：`/setup`
- 应用详情：`/applications/[appid]`
- 实例详情：`/applications/[appid]/instances/[instance_key]`
- 可备份性详情：`/applications/[appid]/instances/[instance_key]/capability`
- 新建计划：`/plans/new`
- 编辑计划：`/plans/[plan_id]`
- 批次详情：`/tasks/batches/[batch_id]`
- 实例任务详情：`/tasks/jobs/[job_id]`
- 快照详情：`/backups/[snapshot_id]`
- 文件索引浏览：`/backups/[snapshot_id]/files`
- 告警详情：`/alerts/[alert_id]`
- 环境诊断：`/settings/diagnostics`

## 9. 全局布局与公共组件

### 9.1 应用框架组件

- `AppShell`：桌面端左侧导航、移动端抽屉导航和内容区域。
- `SidebarNavigation`：主菜单、当前菜单高亮和折叠状态。
- `TopHeader`：产品名称、全局搜索、运行任务、语言切换、告警入口和管理员信息。
- `GlobalTaskIndicator`：展示运行中任务数、排队数、当前吞吐和进入任务中心按钮。
- `NotificationBell`：展示未读告警数量和最近告警。
- `LanguageSwitcher`：跟随系统、简体中文、English。
- `Breadcrumbs`：展示应用、实例、批次和快照层级。

### 9.2 数据列表组件

- `VirtualDataTable`：支持上万条记录的虚拟滚动、服务端分页和列配置。
- `FilterBar`：搜索、状态、用户、应用类型、数据库类型和时间筛选。
- `BulkActionBar`：批量立即备份、批量创建计划、批量重新检测。
- `StatusBadge`：统一展示应用能力、保护状态、任务状态和快照健康状态。
- `OwnerBadge`：展示用户昵称与 UID，昵称只用于显示。
- `DeployIdCell`：显示缩略 `deploy_id`、完整复制和实例类型。
- `EmptyState`：无数据、无计划、无记录等中性状态。
- `BlockedState`：权限、数据库、系统应用等阻断状态。

### 9.3 反馈与危险操作组件

- `ConfirmDialog`：删除计划、删除快照、取消任务等二次确认。
- `CapabilityBlockDialog`：尝试备份不可备份实例时展示原因。
- `UnsupportedDatabaseDialog`：展示数据库类型、检测路径和 V1 边界。
- `NoDataDialog`：说明纯前端应用或空 `appvar` 无需备份。
- `Toast`：短时成功、失败和排队提示。
- `ErrorPanel`：展示本地化错误码、追踪 ID、重试和诊断入口。
- `ProgressDrawer`：不离开当前页面查看批次与任务进度。

## 10. 首次启动向导

首次启动向导在产品尚未完成初始化时强制显示。

### 10.1 欢迎与能力说明

功能：

- 说明产品只备份 `appvar` 中的普通文件和标准 SQLite。
- 说明不支持中型数据库、系统应用和 LightOS。
- 说明 V1 不会写回其他应用。
- 展示备份数据将写入懒猫网盘，而不是备份容器。

组件：能力范围卡片、权限说明卡片、安全边界提示。

### 10.2 权限检测

功能：

- 检查 `appvar.other.read`。
- 检查 `document.write`。
- 检查可选 `user.notify`。
- 验证当前访问者为管理员。
- 未授权必需权限时禁止继续。

组件：权限检查列表、权限状态徽标、重新检查按钮、错误详情抽屉。

### 10.3 网盘存储配置

功能：

- 使用当前登录用户的网盘身份，不提供其他用户 UID 选择器。
- 创建或检测当前用户网盘中的默认根目录 `LazycatAppBackup`。
- 检查读写、重命名、删除临时文件和剩余空间。
- 明确显示真实容器路径和网盘中的展示位置。

组件：用户选择器、存储路径卡片、容量条、读写测试结果、重新检测按钮。

### 10.4 应用与实例扫描

功能：

- 获取所有应用和部署实例。
- 获取每个实例的 `deploy_id`、owner、运行状态和版本。
- 探测跨应用 `appvar` 读取入口。
- 检查目录只读性。
- 扫描空目录、普通文件、标准 SQLite 和不支持数据库特征。
- 按可备份、无数据、不支持、权限受限分类。

组件：扫描进度、分类统计卡片、问题实例表格、查看详情按钮。

### 10.5 性能策略

功能：

- 自动检测 CPU、内存和存储吞吐能力。
- 给出元数据扫描并发、归档并发和 SQLite 快照并发建议。
- 设置全局最大并发和读写限速。
- 默认启用资源自适应，不允许无限并发。

组件：资源检测卡片、并发滑块、限速输入、风险提示。

### 10.6 首个备份

功能：

- 选择一个可备份实例，推荐 Notus。
- 创建默认每日计划或仅执行一次。
- 执行首个备份并显示实时阶段。
- 成功后展示网盘目录、文件数、大小、校验和和耗时。

组件：应用实例选择器、默认计划卡片、任务进度时间线、结果卡片。

## 11. 概览页

概览页回答：当前保护情况如何、任务是否正常、最需要处理什么。

### 11.1 核心指标卡

- 已发现应用数量。
- 已发现实例数量。
- 可备份实例数量。
- 已保护实例数量。
- 无数据实例数量。
- 不支持或受限实例数量。
- 最近 24 小时成功、失败和警告任务数。
- 当前排队、运行和暂停任务数。
- 网盘备份占用和可用空间。

组件：指标卡组、趋势箭头、点击下钻。

### 11.2 保护状态分布

状态：

- 正常保护。
- 待首次备份。
- 即将过期。
- 已过期。
- 未保护。
- 配置异常。
- 无数据。
- 不支持。

组件：分布图、图例、状态筛选跳转。

### 11.3 当前吞吐与队列

展示：

- 当前读取速度、写入速度和压缩速度。
- 运行工作数、排队工作数和预计清空队列时间。
- 小型任务队列、大型任务队列、SQLite 队列。
- 当前资源限制是否触发。

组件：吞吐卡片、队列条、资源限制状态、进入任务中心按钮。

### 11.4 下一批任务

展示未来 24 小时批次：

- 计划名称。
- 计划时间和备份窗口。
- 预计实例数。
- 预计总数据量。
- 上次批次结果。
- 是否包含动态新增实例。

操作：立即执行、暂停计划、查看计划。

### 11.5 风险提醒

示例：

- Notus 尚未保护。
- 某实例连续失败。
- 检测到不支持数据库。
- 网盘剩余空间不足。
- 备份应用未开启自启动。
- `deploy_id` 已变化。
- 部分任务超过备份窗口。
- 存在未清理的 `.partial` 目录。

### 11.6 最近活动

按时间倒序展示：计划变更、批次开始、任务成功或失败、快照校验、存储清理和权限变化。

## 12. 应用与实例页

应用页需要同时支持“按应用聚合”和“按实例展开”两种视图。

### 12.1 顶部标签页

- 全部。
- 可备份。
- 已保护。
- 未保护。
- 无应用数据。
- 数据库不支持。
- 系统或权限受限。
- 需要重新检测。

### 12.2 应用聚合视图

每行展示：

- 应用图标、应用名称和 `appid`。
- 当前版本。
- 单实例或多实例。
- 实例总数。
- 可备份、已保护、无数据和受限实例数。
- 总估算数据量。
- 最近成功备份。
- 下次计划备份。

操作：查看实例、批量创建计划、立即备份全部可备份实例、重新扫描。

### 12.3 实例视图

每行展示：

- 应用名称。
- owner 昵称与 UID。
- 完整或缩略 `deploy_id`。
- 运行状态。
- 可备份性。
- 数据类型摘要。
- 数据量和文件数。
- 保护状态。
- 最近成功备份。
- 下次执行时间。

操作：立即备份、创建计划、查看详情、查看记录、重新检测。

### 12.4 搜索与筛选

- 应用名称、`appid`、`deploy_id`、owner UID。
- 单实例、多实例、内置应用。
- 运行状态。
- 可备份性和保护状态。
- 是否包含 SQLite。
- 是否检测到不支持数据库。
- 数据大小区间。
- 最近备份时间。

### 12.5 批量操作

- 批量立即备份。
- 批量创建计划。
- 批量重新检测。
- 批量暂停或启用计划。

批量操作先生成预检结果，自动排除无数据和不支持实例，并展示最终会创建的任务数。

## 13. 应用详情与实例详情

### 13.1 应用详情页

页面区域：

- 应用基本信息。
- 实例统计。
- 保护状态汇总。
- 数据量趋势。
- 所有实例表格。
- 关联计划。
- 最近批次与告警。

组件：应用标题区、统计卡、实例虚拟表格、趋势图、计划卡片、告警列表。

### 13.2 实例详情页

页面区域：

#### 基本信息

- 应用名称、`appid`、版本。
- owner UID 和昵称。
- `deploy_id`。
- 单实例或多实例。
- 运行状态。
- 最近同步时间。

#### 数据能力

- `appvar` 读取状态。
- 可备份性状态。
- 普通文件数和估算大小。
- SQLite 数量和路径摘要。
- 已忽略特殊文件数量。
- 不支持数据库类型与检测路径。
- 最近检测时间。

#### 数据范围预览

- 目录树预览。
- 文件类型统计。
- 最大文件列表。
- 默认排除项。
- 不展示文件正文。

#### 保护状态

- 启用中的计划。
- 最近成功、失败和无变化快照。
- 下次执行时间。
- 历史成功率。
- 最近快照大小。

#### 操作

- 立即备份。
- 创建或编辑计划。
- 查看任务记录。
- 查看备份库。
- 重新检测。

### 13.3 可备份性详情页

状态及表现：

| 状态 | 页面说明 | 可用操作 |
| --- | --- | --- |
| 可备份 | 存在普通文件或标准 SQLite | 创建计划、立即备份、重新检测 |
| 无应用数据 | 空 `appvar` 或只有可忽略文件 | 返回、重新检测 |
| 数据库不支持 | 检测到中型或文件数据库 | 查看路径、返回、重新检测 |
| 数据不在 appvar | 核心数据位于其他存储域 | 查看边界、返回 |
| 未投影 | 没有找到跨应用只读入口 | 重新检测、诊断 |
| 权限受限 | 读取被系统拒绝 | 检查权限、诊断 |
| 系统应用不支持 | 系统组件或特殊对象 | 返回 |
| LightOS 不支持 | 入口数据不等于系统盘 | 返回 |
| 检测失败 | 探测异常或结果不唯一 | 重试、诊断 |

## 14. 备份计划页

### 14.1 计划列表

字段：

- 计划名称。
- 目标类型：单实例、应用全部实例、动态实例、批量应用。
- 目标实例数量。
- 状态。
- 执行频率。
- 下次计划时间。
- 备份窗口。
- 最近批次结果。
- 保留策略。
- 连续失败次数。

操作：新建、编辑、立即执行、暂停、启用、复制、查看批次、删除。

### 14.2 新建或编辑计划向导

#### 步骤一：选择目标

支持：

- 具体实例。
- 某应用当前全部可备份实例。
- 某应用当前及未来新增实例。
- 多个应用或实例。

组件：应用搜索器、实例虚拟列表、动态目标开关、选择结果摘要。

#### 步骤二：预检与备份范围

功能：

- 批量检测目标状态。
- 显示普通文件、SQLite、未知数据库和不支持数据库。
- 默认备份整个 `appvar` 中允许的数据。
- 展示默认忽略项。
- 不允许用户填写宿主机绝对路径。

组件：预检进度、范围树、数据库兼容性表、阻断项列表。

#### 步骤三：执行时间

支持：

- 仅手动。
- 每小时。
- 每天指定时间。
- 每周指定日期和时间。
- 自定义五段 Cron。

配置：

- 时区。
- 备份窗口时长。
- 错过任务后是否补跑。
- 最大补跑延迟。
- 未来五次执行时间预览。

组件：频率选择器、时间选择器、Cron 编辑器、时区选择器、未来执行列表。

#### 步骤四：性能策略

配置：

- 使用全局自适应策略或计划自定义策略。
- 最大同时运行实例数。
- 小型和大型任务优先级。
- 单实例超时。
- 批次截止时间。
- 读取和写入限速。

组件：策略卡片、并发滑块、限速输入、备份窗口风险预估。

#### 步骤五：保留策略

配置：

- 最近若干份完整快照。
- 每日、每周、每月保留数量。
- 无变化引用快照保留天数。
- 定期强制完整快照周期。
- 最少保留一份完整且校验通过的快照。

组件：保留策略表单、时间线预览、预计空间说明。

#### 步骤六：通知与失败处理

配置：

- 首次失败通知。
- 连续失败升级阈值。
- 批次超出备份窗口通知。
- 存储空间不足通知。
- 成功通知，默认关闭。
- 自动重试次数和退避间隔。

#### 步骤七：确认

展示目标、预计实例数、范围、时间、窗口、性能策略、保留策略、通知和阻断项。

保存操作：

- 保存并立即执行。
- 仅保存。

## 15. 任务中心

任务中心包含“运行队列”“备份批次”“任务历史”三个标签页。

### 15.1 运行队列

展示：

- 排队位置。
- 优先级。
- 应用、owner 和 `deploy_id`。
- 预计大小。
- 任务类型：普通、SQLite、校验、清理。
- 排队原因。
- 预计开始时间。

操作：提高或降低优先级、取消、暂停全局队列、恢复队列。

组件：队列统计卡、虚拟队列表、资源池状态、暂停确认弹窗。

### 15.2 备份批次

字段：

- 批次 ID。
- 计划名称。
- 计划时间。
- 任务总数。
- 成功、无变化、警告、失败、跳过、运行、排队数量。
- 实际开始和完成时间。
- 是否超过备份窗口。

批次详情组件：

- 批次进度环。
- 状态分布。
- 吞吐趋势。
- 用户和应用分组。
- 失败任务列表。
- 实例任务虚拟表格。
- 暂停、恢复和取消剩余任务。

### 15.3 任务历史

筛选：时间、计划、批次、应用、用户、状态、触发方式和数据库类型。

字段：

- 计划时间。
- 实际开始和结束时间。
- 应用、owner、`deploy_id`。
- 触发方式。
- 状态。
- 快照类型。
- 源数据量、归档大小、文件数、耗时。
- 输出时间目录。

### 15.4 实例任务详情

区域：

- 任务摘要。
- 执行阶段时间线。
- 文件扫描统计。
- SQLite 快照统计。
- 归档分片和校验和。
- 资源使用和吞吐。
- 重试记录。
- 结构化日志摘要。
- 输出目录链接。

执行阶段：

1. 任务入队。
2. 获取任务租约和实例锁。
3. 权限与路径预检。
4. 数据分类和数据库检测。
5. 检查网盘空间。
6. 创建 SQLite 快照。
7. 流式生成归档分片。
8. 写入索引和校验和。
9. 完成原子提交。
10. 更新保留策略和通知。

## 16. 备份库

### 16.1 浏览方式

- 按应用树：应用名称 → owner → `deploy_id` → 备份时间。
- 按时间：日期 → 批次 → 应用实例。
- 按用户：owner UID → 应用 → `deploy_id` → 备份时间。

物理目录以“按应用树”为准；其他视图由控制数据库生成。

### 16.2 快照列表

字段：

- 快照时间。
- 应用版本。
- owner 和 `deploy_id`。
- 完整快照或无变化引用快照。
- 文件数、原始大小和归档大小。
- SQLite 数量。
- 完整性状态。
- 最近校验时间。
- 是否受保留策略保护。

### 16.3 快照详情

展示：

- 应用、版本、owner、`deploy_id`。
- 计划时间、实际快照时间和完成时间。
- 网盘路径。
- 归档分片。
- 文件索引。
- SQLite 快照列表。
- 排除和跳过项。
- SHA-256 校验结果。
- 基础快照引用关系。
- 任务和批次来源。

操作：

- 执行快速校验。
- 执行完整校验。
- 浏览文件索引。
- 导出恢复副本到网盘。
- 查看来源任务。
- 根据保留规则删除。

### 16.4 文件索引浏览

组件：路径树、文件表格、类型筛选、名称搜索、大小和时间筛选、校验和展示。

V1 不直接在线预览文件正文，防止泄漏应用敏感内容。

### 16.5 导出恢复副本

V1 可以将归档解压到备份应用网盘中的独立恢复目录，用于管理员检查或手工恢复：

- 不写入目标应用。
- 不覆盖已有恢复目录。
- 展示所需空间和预计文件数。
- 导出完成后执行数量和校验和比对。

## 17. 存储页

### 17.1 存储概览

展示：

- 当前网盘归属 UID。
- 备份根目录。
- 可用空间和已用空间。
- 完整快照数量。
- 无变化引用快照数量。
- `.partial` 目录数量。
- 最近写入和最近校验时间。

### 17.2 目录结构

默认使用稳定、与界面语言无关的 `LazycatAppBackup` 根目录，物理结构固定为“计划备份时间点 → 实例 → 应用”：

```text
/lzcapp/document/LazycatAppBackup/
  <scheduled_at>[__<batch_short_id>]/
    batch.json
    <deploy_id 或 shared-instance-key>/
      <安全应用名称>__<appid>/
        manifest.json
        file-index.jsonl.zst
        files-000001.tar.zst
        files-000002.tar.zst
        sqlite/
          <编码后的相对路径>.sqlite
        checksums.sha256
        warnings.json
        COMPLETED
  _partial/
  _restore_exports/
  _trash/
```

`scheduled_at` 是该批次的逻辑计划时间，格式固定为可排序的 UTC 时间：

```text
20260824T123045.123Z
```

同一批次中的所有实例共享这个顶层时间目录；只有同一毫秒出现目录冲突时才追加批次短 ID。由于成百上千个实例不可能在物理上同时完成快照，`manifest.json` 还必须分别记录实际开始时间、SQLite 快照捕获时间和完成时间。页面按用户当前时区显示可读时间，物理目录始终使用 UTC。owner UID、owner 昵称快照和单/多实例类型写入 `batch.json`、`manifest.json` 和控制库，用于界面按账户分组，不额外改变这棵物理目录树。

### 17.3 存储健康

检查：

- 根目录可读写。
- 临时目录创建、重命名和删除。
- 剩余空间。
- 文件系统错误。
- 未完成目录。
- 孤立快照和丢失分片。

### 17.4 存储维护

操作：

- 立即扫描存储。
- 清理过期 `.partial`。
- 执行保留策略。
- 清理回收站。
- 重建备份库索引。
- 查看空间占用最大的应用。

## 18. 告警页

### 18.1 告警级别

- 信息。
- 警告。
- 严重。
- 紧急。

### 18.2 告警类型

- 必需权限缺失。
- appvar 投影消失。
- 实例 `deploy_id` 变化或消失。
- 检测到不支持数据库。
- SQLite 在线快照失败。
- 文件持续变化导致严格备份失败。
- 网盘不可写或空间不足。
- 批次超过备份窗口。
- 连续任务失败。
- 快照校验失败。
- `.partial` 或孤立目录异常。
- 备份应用未开启自启动。
- 控制数据库异常。

### 18.3 告警操作

- 查看关联应用、任务、批次或快照。
- 立即重试。
- 重新检测。
- 查看修复建议。
- 标记已读。
- 确认已处理。
- 临时静默同类告警。

## 19. 设置页

### 19.1 常规设置

- 页面语言。
- 默认时区。
- 默认备份时间。
- 默认备份窗口。
- 默认任务超时。
- 是否展示高级设置。

### 19.2 调度与性能

- 元数据扫描并发。
- 普通归档并发。
- SQLite 快照并发。
- 校验并发。
- 读取和写入限速。
- 小型任务阈值。
- 大型任务并发上限。
- 任务优先级规则。
- 漏跑补偿和最大延迟。
- 自动重试次数和退避策略。

### 19.3 备份引擎

- 压缩级别。
- 归档分片大小。
- 普通文件严格模式。
- 文件变化重试次数。
- 定期强制完整快照周期。
- 默认排除规则。
- SQLite busy 超时和重试次数。

### 19.4 存储设置

- 网盘归属 UID。
- 根目录状态。
- 空间告警阈值。
- `.partial` 保留时长。
- 回收站保留时长。
- 自动校验周期。
- 自动清理时间窗口。

V1 不支持任意宿主机路径。

### 19.5 通知设置

- 系统通知开关。
- 首次失败通知。
- 连续失败升级。
- 空间不足通知。
- 超出备份窗口通知。
- 校验失败通知。
- 成功通知，默认关闭。
- 每日摘要，默认关闭。

### 19.6 权限与环境

展示：

- `appvar.other.read`。
- `document.write`。
- `user.notify`。
- 当前应用 `deploy_id`。
- 当前存储 UID。
- appvar 读取入口探测结果。
- 已发现应用和实例数量。
- 自启动状态。
- 系统和应用版本。

操作：重新检查权限、重新扫描应用、重新探测、进入环境诊断。

### 19.7 诊断与日志

- 最近错误。
- 调度器状态。
- 队列租约状态。
- 工作池状态。
- 网盘写入测试。
- SQLite 测试快照。
- 下载脱敏诊断包。
- 清理历史日志。

诊断包不得包含文件正文、用户令牌、数据库内容或完整私有路径。

### 19.8 关于

- 产品版本。
- LPK 包 ID。
- 开源许可。
- 项目主页。
- 能力边界。
- 官方文档链接。

## 20. 专用空状态与阻断弹窗

### 20.1 无应用数据页面

适用：纯前端应用、空 `appvar`、仅包含忽略项。

内容：

- “该应用当前没有需要备份的应用数据”。
- 说明静态打包资源不属于运行时数据。
- 最近检测时间。
- 重新检测。

不产生严重告警，不计入未保护比例。

### 20.2 不支持数据库弹窗

内容：

- 检测到的数据库类型。
- 相对路径。
- V1 不支持原因。
- 当前实例不会执行备份。
- 后续数据库适配器规划。

操作：查看详情、重新检测、返回。

不提供强制备份按钮。

### 20.3 系统或特殊应用页面

适用：懒猫网盘、懒猫相册、LightOS、内置系统服务等无法读取或数据不在普通 `appvar` 的对象。

内容：当前可见数据范围、不可覆盖的数据域、是否允许重新检测。

### 20.4 权限或投影缺失弹窗

内容：缺失权限、读取入口状态、追踪 ID、重新检查和环境诊断入口。

## 21. 核心业务流程

### 21.1 应用发现

1. 后端首次全量同步应用和实例。
2. 根据 `deploy_id` 建立实例记录。
3. 获取 owner 用户信息。
4. 订阅应用安装、卸载和实例变化事件。
5. 对新增或变化实例执行能力检测。
6. 前端通过分页和虚拟列表展示。

### 21.2 手动批量备份

1. 管理员选择多个应用或实例。
2. 系统预检并排除无数据和不支持实例。
3. 管理员确认最终实例数和预计数据量。
4. 系统创建一个批次和多个实例任务。
5. 任务按资源策略进入队列。
6. 每个任务独立产生快照或失败记录。
7. 批次完成后汇总通知。

### 21.3 定时批量备份

1. 调度器提前解析即将到期的计划。
2. 在计划时间重新获取动态实例列表。
3. 在单次事务中创建批次和实例任务。
4. 所有任务保存相同 `scheduled_at`。
5. 工作池按照优先级、大小和资源限制执行。
6. 每个实例目录使用实际 `captured_at` 作为时间子目录。
7. 超过备份窗口的任务仍可按策略继续、暂停或跳过，并产生提示。

### 21.4 SQLite 在线备份

1. 通过文件头识别标准 SQLite。
2. 不把原始 `.db`、`-wal`、`-shm` 当作普通文件复制。
3. 在 `/lzcapp/cache` 创建受限临时快照。
4. 使用 SQLite Online Backup API 分页复制。
5. 执行 `quick_check`。
6. 将一致性副本写入归档。
7. 删除临时文件。
8. 失败时不降级为直接复制。

### 21.5 不支持数据库

1. 元数据扫描发现数据库特征。
2. 任务在写入归档前终止。
3. 实例能力状态更新为“数据库不支持”。
4. 产生可操作告警。
5. 不创建成功快照。

### 21.6 无变化快速路径

1. 计算当前目录元数据指纹。
2. 与上一份成功快照比较。
3. 未变化时创建轻量引用快照目录。
4. 引用上一份完整快照，不重复写入归档。
5. 达到强制完整周期后仍创建完整快照。

### 21.7 微服或应用重启

1. 将过期租约任务恢复为可重试或异常中止。
2. 读取未完成批次。
3. 扫描 `.partial` 目录。
4. 根据补跑窗口恢复任务。
5. 已存在 `COMPLETED` 的输出不得重复写入。

## 22. 状态定义

### 22.1 可备份性状态

- `BACKUPABLE`
- `NO_DATA`
- `UNSUPPORTED_DATABASE`
- `UNKNOWN_DATABASE`
- `DATA_OUTSIDE_APPVAR`
- `APPVAR_NOT_PROJECTED`
- `PERMISSION_DENIED`
- `SYSTEM_UNSUPPORTED`
- `LIGHTOS_UNSUPPORTED`
- `PROBE_FAILED`

### 22.2 任务状态

- 等待中。
- 已租约。
- 预检中。
- 扫描中。
- SQLite 快照中。
- 归档中。
- 校验中。
- 提交中。
- 成功。
- 无变化。
- 成功但有警告。
- 失败。
- 已取消。
- 已超时。
- 已跳过。
- 异常中止。

### 22.3 快照状态

- 写入中。
- 已完成未校验。
- 校验通过。
- 校验警告。
- 校验失败。
- 引用快照。
- 待清理。
- 已移入回收站。

## 23. 数据与目录规则

### 23.1 实例唯一性

多实例应用以 `appid + deploy_id` 唯一定位实例，同时保存 `owner_uid` 作为账户归类键。单实例应用若平台未返回 `deploy_id`，使用稳定的 `shared:<appid>` 生成 `instance_key`；目录中使用安全化后的 shared key，但清单中明确标记它不是平台 deploy ID。owner 昵称只用于展示，不参与唯一性判断。

### 23.2 时间规则

- 控制数据库统一保存 UTC。
- 页面按用户时区显示。
- 顶层网盘目录使用批次 `scheduled_at`，格式为 `yyyyMMdd'T'HHmmss.SSS'Z'`，不包含冒号。
- 计划时间、实际开始时间、SQLite 快照捕获时间和完成时间分别记录。
- 同一计划时间点展开的所有实例任务共享同一个顶层时间目录。

### 23.3 目录命名

- 第一级：批次计划时间 `scheduled_at`。
- 第二级：安全化后的 `deploy_id`；单实例缺少 deploy ID 时使用安全化的 shared instance key。
- 第三级：安全化后的应用名称加完整 `appid` 或其稳定短后缀。
- owner UID 和昵称写入清单及索引，在界面中按账户分组，不插入物理目录层级。
- 名称统一做 Unicode 规范化，移除路径分隔符和控制字符，超长时截断并追加稳定短哈希。

### 23.4 完成标记

只有归档分片、SQLite 快照、索引和校验和全部写入成功后才最后创建 `COMPLETED`。没有完成标记的目录不进入正常备份库。

### 23.5 计划删除

删除计划不删除快照。实例或应用卸载后，历史快照继续保留，计划进入异常或归档状态。

### 23.6 保留策略

- 只在新快照成功后执行。
- 不删除唯一完整且校验通过的快照。
- 引用快照存在时保留其基础完整快照。
- 校验失败时暂停自动清理。
- 删除先进入 `_trash`，达到宽限期后物理删除。

## 24. 非功能要求

### 24.1 可扩展性

设计验收目标：

- 应用目录支持至少 10,000 条实例记录。
- 单批次支持至少 5,000 个实例任务。
- 到期任务入队不创建无上限 goroutine。
- 列表采用服务端游标分页和前端虚拟滚动。
- 任务详情日志和文件索引不全部存入控制数据库。
- 大批量任务按小型、普通、大型和 SQLite 工作池调度。

### 24.2 性能

- 基准设备上，同一时间点 5,000 个实例任务应在 10 秒内完成批次展开和持久化创建；该指标不包含文件扫描和上传时间。
- 默认归档并发由设备能力自动计算，并允许管理员设置上限。
- 单个工作任务使用有界缓冲，不能随文件大小增长内存。
- 归档直接流式写入网盘。
- UI 在 10,000 条实例和 100,000 条历史任务下仍能分页浏览。
- 总完成时间受实际数据量和存储吞吐约束，产品不得展示无法兑现的固定完成时间。

### 24.3 可靠性

- 计划、批次、任务和租约持久化。
- 批次创建和任务创建具备幂等键。
- 任务崩溃后可恢复。
- 失败不会删除旧快照。
- 部分写入目录不会显示为成功。
- SQLite 失败不降级为直接复制。

### 24.4 安全

- 只读访问其他应用。
- 不接受任意绝对源路径。
- 不跟随越过源根目录的符号链接。
- 不备份特殊设备和 Socket。
- 文件正文不进入日志和诊断包。
- 所有接口要求管理员身份。
- 前端和后端同源访问，不开放公共 API。

### 24.5 国际化

- `zh-CN` 与 `en-US` 覆盖率 100%。
- 页面、菜单、状态、错误、通知、时间和容量全部本地化。
- 后端返回稳定错误码和参数，不返回用于产品展示的硬编码中文。
- 切换语言不影响筛选、表单草稿和任务状态。

### 24.6 可用性与无障碍

- 关键状态同时使用图标和文字。
- 所有表单具有错误说明。
- 键盘可完成主要操作。
- 颜色对比满足管理端可读性要求。
- 移动端至少支持查看状态、告警和任务详情；复杂计划编辑以桌面端为主。

## 25. V1 验收标准

### 25.1 权限和发现

- 能检测 `appvar.other.read` 和 `document.write`。
- 能获取应用、`deploy_id`、owner 和多实例状态。
- 能识别备份应用自身并排除递归备份。
- 能识别空 `appvar`、无投影和权限拒绝。

### 25.2 数据类型

- 能备份普通文件和目录。
- 能在应用运行时创建标准 SQLite 一致性副本。
- 不直接复制 SQLite WAL/SHM。
- 能阻断 MySQL、PostgreSQL、MongoDB、Redis。
- 能阻断至少一组常见文件数据库特征。
- 能跳过 Socket、命名管道和设备文件。

### 25.3 网盘存储

- 备份数据写入 `/lzcapp/document/LazycatAppBackup`。
- 目录树严格按 `scheduled_at → deploy_id/instance_key → 应用名称` 组织；owner 信息写入清单并用于页面分组。
- 容器 `/lzcapp/var` 不出现长期备份归档。
- 失败任务只产生可识别的 `.partial`，不会进入正常备份库。

### 25.4 调度与规模

- 支持单实例、动态多实例和批量应用计划。
- 同一时间点可创建至少 5,000 个任务记录。
- 同一 `deploy_id` 不会并发执行两个备份。
- 任务按资源限制稳定执行。
- 重启后可以恢复队列和补跑。

### 25.5 页面

- 八个主菜单（概览、应用与实例、备份计划、任务中心、备份库、存储、告警、设置）和所有详情页可用。
- 无数据、不支持数据库、系统应用和权限异常拥有独立页面或弹窗。
- 应用、任务和快照列表支持分页、筛选和虚拟滚动。
- 批次详情能展示上千个实例任务。

### 25.6 校验与恢复准备

- 快照包含清单、文件索引、分片校验和和完成标记。
- 能执行快速和完整校验。
- 能将快照导出到网盘恢复目录。
- 产品中不存在直接写回目标应用的入口。

### 25.7 国际化

- 中文和英文菜单、页面、状态、错误和通知一致。
- LPK 应用名称和描述支持中文与英文。

## 26. 后续功能规划

### 26.1 数据库适配器

- MySQL 逻辑备份和物理备份适配器。
- PostgreSQL `pg_dump`、`pg_basebackup` 适配器。
- MongoDB `mongodump` 适配器。
- Redis RDB/AOF 安全备份适配器。
- DuckDB、LMDB、RocksDB、LevelDB、bbolt、BadgerDB 等专用快照适配器。

每个适配器必须使用数据库官方备份机制，不能把运行中数据目录直接复制后标记为一致备份。

### 26.2 应用备份协议

- 应用 `prepare`、`export`、`complete` 钩子。
- Notus 暂停索引写入和 flush 的专用适配器。
- 应用开发者声明备份数据范围和数据库类型。
- 商店兼容性目录和签名规则在线更新。

### 26.3 恢复能力

- 独立发行、额外申请 `appvar.other.write` 的恢复组件。
- 目标应用停机、原目录回滚副本、恢复、校验和重新启动流程。
- 按文件、目录和数据库选择性恢复。
- 恢复演练和定期自动验证。

### 26.4 存储与灾备

- 用户选择任意懒猫网盘目录。
- SFTP、WebDAV、S3、NAS 和异地微服目标。
- 多目标同时写入。
- 客户端加密和密钥导出。
- 跨设备复制和灾难恢复。

### 26.5 高级性能

- 文件级增量归档。
- 内容寻址、分块去重和压缩字典。
- 变化日志或文件系统快照能力。
- 多磁盘并发和存储拓扑感知。
- 大型应用专用窗口和带宽配额。

### 26.6 系统与特殊应用

- 与懒猫网盘、懒猫相册官方接口协作。
- LightOS 官方实例快照接口。
- 系统设置与应用部署配置备份。

### 26.7 后续新增菜单与页面

后续能力默认不在 V1 主导航中展示，只有相应技术能力、权限模型和恢复安全流程成熟后才开放。

| 后续菜单或页面 | 主要功能 | 主要组件 |
| --- | --- | --- |
| 恢复中心 | 选择快照、选择目标实例、恢复前检查、停机提示、回滚副本、恢复任务和恢复报告 | `RestoreWizard`、`TargetInstancePicker`、`PreRestoreChecklist`、`RollbackPlanCard`、`RestoreProgressTimeline` |
| 恢复演练 | 在隔离目录或测试实例中定期验证快照可用性，输出演练报告 | `DrillPlanTable`、`DrillReportViewer`、`IntegrityComparison` |
| 兼容性中心 | 展示应用和数据库适配器、兼容版本、检测规则、验证状态和更新记录 | `AdapterCatalog`、`CompatibilityMatrix`、`SignatureRuleViewer`、`AdapterTestPanel` |
| 灾备目标 | 配置 S3、WebDAV、SFTP、NAS、异地微服及多目标写入策略 | `StorageTargetCard`、`ConnectionTestDialog`、`ReplicationPolicyForm` |
| 加密与密钥 | 配置客户端加密、密钥轮换、恢复密钥导出和密钥可用性检查 | `EncryptionPolicyForm`、`KeyStatusPanel`、`RecoveryKeyDialog` |
| 应用备份协议 | 管理应用 `prepare/export/complete` 钩子、签名声明和适配器测试 | `BackupHookRegistry`、`HookTestRunner`、`CapabilityDeclarationViewer` |

### 26.8 后续页面的安全门槛

- “恢复中心”必须独立申请更高权限，不能因备份应用升级而默认获得写入其他 appvar 的能力。
- 数据库适配器必须通过真实恢复测试后才从“实验性”转为“正式支持”。
- 灾备目标必须通过连接、写入、读取、删除和完整性校验 POC。
- 加密功能必须提供密钥丢失风险说明和离线恢复密钥导出。
- 系统应用和 LightOS 只使用官方接口，不通过宿主机路径绕过权限。

## 27. 关键产品决策

1. 文档版本固定为 V1。
2. 前端使用 Next.js，后端使用 Go。
3. V1 只正式支持普通文件和标准 SQLite。
4. 已知中型数据库和其他文件数据库一律阻断。
5. 备份归档直接写入懒猫网盘。
6. 多实例以 `deploy_id` 为核心，owner UID 用于分类。
7. 同一时间点形成批次，实际执行采用受控并发和备份窗口。
8. 物理目录按批次计划时间点、`deploy_id`/instance key、应用名称组织；owner 信息进入清单和逻辑索引。
9. V1 不直接恢复到目标应用。
10. 跨应用 appvar 的实际读取入口必须先通过真机 POC 验证，不能硬编码宿主机路径。

## 28. 参考资料

- 懒猫 `package.yml` 权限规范：<https://developer.lazycat.cloud/spec/package.html>
- 懒猫 `lzc-manifest.yml` 规范：<https://developer.lazycat.cloud/spec/manifest.html>
- 懒猫文件访问：<https://developer.lazycat.cloud/advanced-file.html>
- 懒猫多实例：<https://developer.lazycat.cloud/advanced-multi-instance.html>
- 懒猫环境变量：<https://developer.lazycat.cloud/advanced-envs.html>
- 懒猫数据库服务：<https://developer.lazycat.cloud/advanced-db.html>
- 懒猫 Lzc SDK：<https://developer.lazycat.cloud/introduction.html>
- SQLite Online Backup API：<https://www.sqlite.org/backup.html>
- Lazycat Skills：<https://github.com/whoamihappyhacking/lazycat-skills>


## 29. 变更记录

| 日期 | 文档版本 | 变更 |
| --- | --- | --- |
| 2026-08-24 | V1 | 按 Next.js + Go、普通文件与 SQLite、懒猫网盘目录树、多实例和大规模调度要求完整重写 |
