# 咪咪应用备份需求文档 PRD

> 文档版本：V1  
> 产品名称：咪咪应用备份（Mimi App Backup）
> LPK 包标识：`mimi-app-backup`
> 产品形态：安装在懒猫微服中的多实例 Web 应用
> 目标平台：懒猫微服 LPK V2  
> 登录方式：懒猫 OIDC
> 支持语言：简体中文（zh-CN）、English（en-US）  
> 最后更新：2026-08-27
> 说明：文档版本固定为 V1，后续调整只更新“最后更新”和变更记录。

## 1. 产品背景

懒猫微服中的应用会把配置、附件、索引、SQLite 数据库和运行状态保存在应用数据目录中。应用升级、程序缺陷、误操作或存储异常都可能造成数据丢失。Notus 的向量索引丢失并需要重新构建，是本项目最直接的使用场景。

咪咪应用备份为当前登录用户发现其可访问的应用实例，执行手动或定时备份，并将结果保存到该用户自己的懒猫网盘。产品不提供跨用户集中管理，也不把其他家庭成员的数据写入管理员网盘。

项目已完成 POC，当前链路可以：

1. 识别当前懒猫用户。
2. 查询当前用户可访问的应用目录。
3. 读取 `appvar.other.read` 对应的运行时应用数据投影。
4. 探测应用数据目录、文件数量、容量和数据库类型。
5. 对普通文件计算 SHA-256。
6. 为符合条件的应用创建一次手动备份。
7. 将备份结果写入当前用户自己的懒猫网盘。

V1 在该链路上增加 OIDC 会话、正式页面、备份计划、任务队列、ZIP 归档、SQLite 一致性快照、备份库、告警和保留策略。

## 2. 产品定位

V1 是面向懒猫用户的应用数据备份工具，覆盖以下数据：

- 普通文件和目录。
- 配置文件、附件、图片、模型文件、向量索引和全文索引。
- 标准 SQLite 3 数据库。
- 当前用户在懒猫平台上下文中可访问的单实例和多实例应用数据。

V1 不承担整机备份、容器镜像备份和通用数据库备份。

## 3. 产品原则

### 3.1 当前用户边界

每个用户登录后只能查看和操作当前 OIDC 用户能够访问的应用实例。应用目录查询不使用管理员跨用户参数，后台也不允许切换用户。

管理员在 V1 中与普通用户使用相同的数据范围：

- 只能查看当前管理员账号可访问的应用实例。
- 只能创建当前管理员账号的数据备份。
- 备份只能写入当前管理员自己的懒猫网盘。
- 不允许查询、备份或恢复其他家庭成员的数据。

### 3.2 多实例备份应用

咪咪应用备份自身使用多实例部署。每个懒猫用户运行独立的应用容器，并拥有独立的：

- 控制数据库。
- 备份计划。
- 任务队列。
- 快照索引。
- 告警和设置。
- 懒猫网盘备份目录。

### 3.3 单实例和多实例目标应用

V1 支持当前用户可访问的单实例与多实例目标应用。

多实例目标应用由平台为不同用户运行独立实例，页面标记为“用户隔离”。

单实例目标应用可能使用共享 `appvar`。V1 允许备份，并在详情和计划中显示“共享实例风险”提示；提示不阻断查看任务、立即备份或创建计划。

平台返回的当前用户应用目录和运行时投影构成 V1 的授权边界。产品不使用管理员接口扩展可见范围。

### 3.4 数据只读

备份过程只读取目标应用数据。V1 不申请 `appvar.other.write`，不提供写回、覆盖或删除目标应用数据的能力。

### 3.5 用户网盘存储

所有长期备份文件写入当前用户自己的懒猫网盘。备份应用容器只保存任务状态、索引和短生命周期临时文件。

## 4. V1 产品目标

1. 所有业务页面必须经过懒猫 OIDC 登录。
2. 登录用户只能看到自己可访问的应用实例。
3. 用户可以查看应用数据大小、文件数量、数据库类型和可备份状态。
4. 用户可以立即备份单个应用。
5. 用户可以批量选择多个应用创建一次性备份。
6. 用户可以创建按小时、每天、每周或 Cron 执行的备份计划。
7. 普通文件在应用运行期间进行受控读取。
8. SQLite 在应用运行期间创建一致性快照。
9. MySQL、PostgreSQL、MongoDB、Redis 等数据库被检测后阻止备份。
10. 每次备份生成 `snapshot.zip` 和 `manifest.json`。
11. 备份目录固定为 `LazycatAppBackup/<时间>/<deploy_id>/`。
12. 用户可以查看任务进度、历史结果、失败原因和快照完整性。
13. 用户可以执行快照校验和导出，不直接恢复到目标应用。
14. 后端可以在同一计划时间快速创建大量任务，并使用有界并发稳定执行。
15. 中文和英文页面、状态、错误和通知保持一致。

## 5. 成功标准

- Notus 可以建立每天自动备份计划。
- Notus 普通文件、索引和 SQLite 数据能够写入当前用户网盘。
- `snapshot.zip` 可以正常解压，内部文件路径与源应用相对目录一致。
- 外部 `manifest.json` 可以独立描述快照、归档校验值和源应用信息。
- 单实例应用在备份前展示共享实例风险。
- 多实例应用只展示当前用户对应的应用实例。
- 普通用户和管理员都无法使用本产品查看其他家庭成员的应用目录与快照。
- 数据库不支持、无数据、权限不足和系统应用均有明确状态。
- 应用重启后可以恢复任务队列，并按补跑规则处理错过的计划。
- 同一时间点创建大量任务时，不出现无界 goroutine、内存陡增或大量文件同时打开。

## 6. V1 非目标

- 管理员跨用户备份。
- 管理员统一查看全体家庭成员的快照。
- 将其他用户备份写入管理员网盘。
- 直接恢复到目标应用 `appvar`。
- 自动停止、启动或重启目标应用。
- MySQL、MariaDB、PostgreSQL、MongoDB、Redis 的正式备份。
- Elasticsearch、OpenSearch、ClickHouse 等服务型数据库备份。
- DuckDB、LMDB、RocksDB、LevelDB、Pebble、bbolt、BadgerDB、H2、HSQLDB、Derby 的一致性备份。
- LightOS 系统盘备份。
- 懒猫网盘、懒猫相册等系统级数据的通用备份。
- 其他应用的用户文稿、媒体目录、RemoteFS、NAS 和外部存储。
- 整机、块设备、容器镜像和操作系统配置备份。
- 本地账号、密码登录和游客模式。
- POC 探测页面作为正式产品页面。
- 任意宿主机路径输入。

## 7. 用户角色和权限

### 7.1 普通用户

普通用户可以：

- 使用懒猫 OIDC 登录自己的备份应用实例。
- 查看自己可访问的应用和实例。
- 查看应用可备份性。
- 执行立即备份。
- 创建和管理自己的备份计划。
- 查看自己的任务、快照、存储和告警。
- 修改自己的备份设置。
- 校验和导出自己的快照。

### 7.2 管理员

管理员在 V1 中拥有与普通用户相同的备份功能。管理员角色仅用于：

- 显示账户角色。
- 记录审计信息。
- 预留后续管理员能力。

管理员不能：

- 使用 `other_uid` 查询其他用户。
- 切换租户。
- 查看其他用户的计划、任务、快照和网盘目录。
- 代表其他用户创建计划。

### 7.3 OIDC 会话

所有业务页面必须存在有效 OIDC 会话。进入业务页面后，系统校验：

```text
session.gateway_uid
=
X-HC-User-ID
```

`session.uid` 是 OIDC UserInfo 的 `preferred_username`，用于显示和审计；它不与懒猫网关 UID 作相等比较。回调保存当次网关注入的 UID，后续请求只使用同一命名空间的 `session.gateway_uid` 做校验。

任一身份缺失或不一致时：

- 清除本地会话。
- 返回 403。
- 显示“当前登录会话与懒猫账号不一致”。
- 不返回任何应用、任务或快照数据。

## 8. 数据支持矩阵

| 数据类型 | V1 行为 | 一致性 | 允许创建计划 |
| --- | --- | --- | --- |
| 普通文件和目录 | 写入 ZIP | 文件级、尽力一致 | 是 |
| JSON、YAML、TOML、CSV、Markdown、文本 | 写入 ZIP | 文件级、尽力一致 | 是 |
| 图片、附件、模型文件 | 写入 ZIP | 文件级、尽力一致 | 是 |
| 向量索引、全文索引、自定义二进制目录 | 写入 ZIP并提示运行时写入风险 | 文件级、尽力一致 | 是 |
| 标准 SQLite 3 | 使用 Online Backup API 后写入 ZIP | SQLite 快照一致 | 是 |
| SQLite WAL、SHM、Journal | 不单独复制 | 由 SQLite 快照覆盖 | 是 |
| SQLCipher、加密 SQLite | 阻止备份 | 不支持 | 否 |
| MySQL、MariaDB | 阻止备份 | 不支持 | 否 |
| PostgreSQL | 阻止备份 | 不支持 | 否 |
| MongoDB | 阻止备份 | 不支持 | 否 |
| Redis RDB、AOF | 阻止备份 | 不支持 | 否 |
| 其他已知文件数据库 | 阻止备份 | 不支持 | 否 |
| 未知数据库特征 | 阻止备份 | 不支持 | 否 |
| Socket、命名管道、设备文件 | 跳过并记录 | 不适用 | 不单独阻断 |
| 空数据目录 | 显示“无应用数据” | 不适用 | 否 |
| 系统级或特殊存储应用 | 显示“不支持” | 不适用 | 否 |

## 9. 可备份性判定

应用满足以下条件时，状态为“可备份”：

1. 当前 OIDC 用户可以在应用目录中看到该应用。
2. 后端目录查询结果与当前 `tenant_uid` 一致。
3. 运行时数据投影能够解析该应用的 `appid` 与 `deploy_id`。
4. 数据目录可以读取。
5. 未发现阻断型数据库。
6. 数据目录至少包含一个允许备份的普通文件或 SQLite。
7. 目标应用需排除备份应用自身。

单实例应用满足上述条件后仍可备份，同时标记“共享实例风险”。

## 10. 业务对象

### 10.1 当前用户

字段包括：

- `tenant_uid`
- OIDC subject
- 显示名称
- 邮箱
- 角色：`NORMAL` 或 `ADMIN`
- 会话时间
- 当前语言
- 当前时区

### 10.2 应用

字段包括：

- 应用名称
- `appid`
- 版本
- 图标
- 是否内置
- 单实例或多实例
- 当前用户可见实例数量
- 可备份状态
- 数据大小
- 文件数量
- SQLite 数量
- 最近备份时间
- 下次计划时间

### 10.3 应用实例

唯一标识：

```text
tenant_uid + appid + deploy_id
```

字段包括：

- `appid`
- `deploy_id`
- 实例模式
- 运行状态
- 数据目录状态
- 可备份性
- 数据类型摘要
- 数据大小
- 文件数量
- 最近检测时间

### 10.4 备份计划

字段包括：

- 计划 ID
- 计划名称
- 目标选择规则
- 执行频率
- 时区
- 备份窗口
- 并发策略
- 重试策略
- 保留策略
- 通知设置
- 状态
- 下次执行时间

### 10.5 备份批次

同一个计划时间点形成一个批次。批次包含一个或多个实例任务，并共享 `scheduled_at`。

### 10.6 实例任务

一个任务只处理一个 `deploy_id`。任务拥有独立状态、执行时间、重试记录、输出路径和错误信息。

### 10.7 快照

快照由两个最终文件组成：

```text
snapshot.zip
manifest.json
```

`manifest.json` 写入完成后，该快照才进入备份库。

## 11. 信息架构

### 11.1 主菜单

| 菜单名称 | 英文名称 | 路由 | 页面目标 |
| --- | --- | --- | --- |
| 概览 | Overview | `/overview` | 查看当前用户保护状态、任务、风险和容量 |
| 应用 | Applications | `/applications` | 查看当前用户可访问的应用、实例和可备份性 |
| 备份计划 | Backup Plans | `/plans` | 创建和管理定时备份 |
| 任务中心 | Task Center | `/tasks` | 查看队列、批次和任务历史 |
| 备份库 | Backup Library | `/backups` | 浏览当前用户的历史快照 |
| 存储 | Storage | `/storage` | 查看当前用户网盘备份占用和维护状态 |
| 告警 | Alerts | `/alerts` | 查看权限、任务、数据库和存储异常 |
| 设置 | Settings | `/settings` | 配置备份、通知、语言、OIDC 会话和环境状态 |

### 11.2 非主菜单页面

- 首次使用向导：`/setup`
- 应用详情：`/applications/[appid]`
- 实例详情：`/applications/[appid]/instances/[deploy_id]`
- 新建计划：`/plans/new`
- 编辑计划：`/plans/[plan_id]`
- 批次详情：`/tasks/batches/[batch_id]`
- 任务详情：`/tasks/jobs/[job_id]`
- 快照详情：`/backups/[snapshot_id]`
- 文件索引：`/backups/[snapshot_id]/files`
- 告警详情：`/alerts/[alert_id]`
- OIDC 登录错误：`/auth/error`

POC 的“应用与数据库全量探测”页面保留在代码仓库中，供开发和排查参考，不进入生产导航、路由表和 PRD 功能范围。

## 12. 全局应用框架

### 12.1 左侧菜单

组件：

- 产品 Logo 和名称。
- 八个主菜单图标。
- 当前页面高亮。
- 折叠按钮。
- 移动端抽屉菜单。

### 12.2 顶部栏

组件：

- 页面标题和面包屑。
- 全局搜索。
- 运行任务指示器。
- 告警入口。
- 语言切换。
- 当前用户头像、名称和角色。
- 退出登录。

### 12.3 公共组件

- `VirtualDataTable`：服务端分页和虚拟滚动。
- `FilterBar`：状态、类型、时间和大小筛选。
- `StatusBadge`：统一状态标签。
- `ModeBadge`：单实例、多实例。
- `DatabaseBadge`：SQLite、未知数据库、不支持数据库。
- `BulkActionBar`：批量立即备份和批量创建计划。
- `ConfirmDialog`：删除、取消、清理。
- `ProgressDrawer`：在当前页面查看批次进度。
- `SessionExpiredDialog`：OIDC 会话过期。
- `SharedInstanceRiskDialog`：单实例共享数据提示。
- `UnsupportedDatabaseDialog`：数据库阻断说明。
- `NoDataState`：无应用数据。
- `ErrorPanel`：错误码、追踪 ID、重试入口。

## 13. OIDC 登录流程

### 13.1 首次访问

1. 用户通过懒猫入口打开应用。
2. 后端检查 OIDC 会话。
3. 没有会话时显示应用登录页；该页不自动发起授权。
4. 用户点击“使用懒猫 OIDC 登录”后，后端创建登录事务并跳转懒猫 OIDC。
5. 用户完成授权后返回 `/auth/oidc/callback`。
6. 后端校验 `state`、`nonce`、授权码和 ID Token。
7. 后端读取用户信息和权限组。
8. 后端保存 OIDC 用户信息和回调请求的懒猫网关 UID，并创建会话。
9. 浏览器进入首页；后续业务请求校验 `session.gateway_uid` 与当前 `X-HC-User-ID`。

### 13.2 会话状态

顶部用户菜单展示：

- 用户名称。
- UID。
- `NORMAL` 或 `ADMIN`。
- 会话到期时间。
- 重新登录。
- 退出登录。

### 13.3 会话异常

- OIDC 回调失败：显示登录错误页。
- 用户身份不一致：清除服务端会话与 Cookie，回到登录页并提示重新使用当前实例所属的懒猫账号授权；不返回业务数据。
- 会话过期：弹窗后重新登录。
- 角色发生变化：刷新会话。
- OIDC 服务暂不可用：保留后台任务，前台显示服务异常。

## 14. 首次使用向导

首次使用向导只面向当前登录用户。

### 14.1 欢迎

展示：

- 支持普通文件和 SQLite。
- 不支持的数据库类型。
- 单实例共享数据提示。
- 备份只写入当前用户网盘。
- V1 不提供直接恢复。

组件：能力卡片、安全说明、开始按钮。

### 14.2 登录与身份检查

检查：

- OIDC 会话有效。
- 当前业务请求的 `session.gateway_uid` 与 `X-HC-User-ID` 一致。
- 显示用户角色。
- 管理员跨用户功能未启用。

组件：身份卡片、状态列表、重新登录按钮。

### 14.3 权限检查

检查：

- `appvar.other.read`
- `document.write`
- 可选 `user.notify`
- 运行时兼容权限
- appvar 数据投影
- 当前用户网盘写入

组件：权限列表、重新检测、错误说明。

### 14.4 应用扫描

功能：

- 查询当前用户可访问应用。
- 排除备份应用自身。
- 扫描数据目录。
- 识别单实例与多实例。
- 识别 SQLite 和不支持数据库。
- 分类可备份、无数据、不支持和检测失败。

组件：扫描进度、统计卡、问题列表。

### 14.5 首次备份

功能：

- 选择一个可备份应用。
- 单实例时显示共享数据风险提示。
- 执行手动备份。
- 显示 ZIP 路径、SHA-256、文件数、大小和耗时。

组件：应用选择器、进度时间线、结果卡片。

## 15. 概览页

### 15.1 指标卡

- 当前用户可见应用数。
- 可备份应用数。
- 已保护应用数。
- 待首次备份。
- 不支持应用数。
- 最近 24 小时成功、失败和警告任务。
- 排队和运行任务。
- 当前用户备份占用。

### 15.2 保护状态分布

状态：

- 正常保护。
- 待首次备份。
- 即将过期。
- 已过期。
- 未保护。
- 无数据。
- 不支持。
- 配置异常。

组件：环形图、图例、下钻筛选。

### 15.3 当前队列与吞吐

展示：

- 当前读取速度。
- ZIP 写入速度。
- SQLite 快照任务。
- 排队任务数。
- 运行任务数。
- 预计队列清空时间。
- 资源限制状态。

### 15.4 下一批计划

展示未来 24 小时：

- 计划名称。
- 执行时间。
- 目标应用数。
- 预计数据量。
- 上次结果。
- 操作：立即执行、暂停、查看。

### 15.5 风险提醒

- 尚未备份。
- 连续失败。
- 检测到不支持数据库。
- 单实例共享数据计划。
- 网盘空间不足。
- OIDC 配置异常。
- appvar 投影不可用。
- 存在未清理临时任务。

### 15.6 最近活动

展示计划变更、备份开始、备份完成、校验、清理、登录和权限变化。

## 16. 应用页

### 16.1 标签页

- 全部。
- 可备份。
- 已保护。
- 未保护。
- 无应用数据。
- 数据库不支持。
- 系统或权限受限。
- 需要重新检测。

### 16.2 应用列表字段

- 应用图标。
- 应用名称。
- `appid`。
- 版本。
- 单实例或多实例。
- `deploy_id`。
- 运行状态。
- 数据大小。
- 文件数量。
- SQLite 数量。
- 可备份性。
- 保护状态。
- 最近备份。
- 下次执行。

### 16.3 筛选

- 应用名称、`appid`、`deploy_id`。
- 单实例、多实例。
- 运行状态。
- 可备份性。
- 保护状态。
- 是否包含 SQLite。
- 数据库类型。
- 数据大小区间。
- 最近备份时间。

不提供用户、owner 和租户筛选。

### 16.4 行操作

- 立即备份。
- 创建计划。
- 查看详情。
- 查看任务。
- 查看快照。
- 重新检测。

### 16.5 批量操作

- 批量立即备份。
- 批量创建计划。
- 批量重新检测。

批量确认弹窗展示：

- 选中应用数。
- 实际可备份数。
- 无数据数。
- 数据库阻断数。
- 单实例风险数。
- 预计总大小。

## 17. 应用详情页

### 17.1 基本信息

- 应用名称、图标、`appid`、版本。
- `deploy_id`。
- 单实例或多实例。
- 运行状态。
- 最近同步时间。

### 17.2 数据概览

- 数据目录状态。
- 文件数量。
- 目录数量。
- 总大小。
- SQLite 数量。
- 跳过项。
- 数据库检测结果。
- 最近检测时间。

### 17.3 数据范围

- 目录树。
- 文件类型统计。
- 最大文件列表。
- SQLite 路径摘要。
- 忽略项。
- 不显示文件正文和源绝对路径。

### 17.4 保护状态

- 关联计划。
- 最近成功快照。
- 最近失败任务。
- 下次执行时间。
- 历史成功率。
- 快照容量趋势。

### 17.5 操作

- 立即备份。
- 创建或编辑计划。
- 查看任务历史。
- 查看备份库。
- 重新检测。

单实例应用在页面顶部展示共享数据风险卡片。

## 18. 备份计划页

### 18.1 计划列表字段

- 计划名称。
- 目标类型。
- 目标应用数。
- 状态。
- 执行频率。
- 下次执行。
- 备份窗口。
- 最近结果。
- 保留策略。
- 连续失败次数。

### 18.2 操作

- 新建。
- 编辑。
- 立即执行。
- 暂停。
- 启用。
- 复制。
- 查看批次。
- 删除。

## 19. 新建或编辑计划向导

### 19.1 选择目标

支持：

- 单个应用实例。
- 当前应用的当前实例。
- 多个应用。
- 当前用户未来新增的可备份应用。

组件：搜索器、虚拟列表、选择摘要。

### 19.2 预检

展示：

- 普通文件。
- SQLite。
- 不支持数据库。
- 无数据。
- 单实例共享数据风险。
- 预计文件数和大小。

单实例目标需要勾选确认。

### 19.3 执行时间

支持：

- 仅手动。
- 每小时。
- 每天。
- 每周。
- 五段 Cron。
- 时区。
- 备份窗口。
- 错过后补跑。
- 最大补跑延迟。
- 未来五次执行预览。

### 19.4 性能策略

- 使用全局自适应。
- 最大并发任务数。
- 单任务超时。
- 批次截止时间。
- 读取限速。
- ZIP 写入限速。
- 大型任务优先级。

### 19.5 保留策略

- 最近若干份。
- 每日、每周、每月保留。
- 最少保留一份校验通过快照。
- 定期强制完整备份。
- 回收站宽限期。

### 19.6 通知和重试

- 首次失败。
- 连续失败升级。
- 超出备份窗口。
- 空间不足。
- 校验失败。
- 成功通知。
- 自动重试次数。
- 退避间隔。

### 19.7 确认

展示全部配置，并提供：

- 保存并立即执行。
- 仅保存。

## 20. 任务中心

任务中心包含三个标签页：

- 运行队列。
- 备份批次。
- 任务历史。

### 20.1 运行队列

字段：

- 队列位置。
- 优先级。
- 应用名称。
- `deploy_id`。
- 预计大小。
- 任务类型。
- 排队原因。
- 预计开始时间。

操作：

- 调整优先级。
- 取消。
- 暂停队列。
- 恢复队列。

### 20.2 备份批次

字段：

- 批次 ID。
- 计划名称。
- 计划时间。
- 任务总数。
- 成功、警告、失败、跳过、运行和排队数。
- 开始时间。
- 完成时间。
- 是否超过窗口。

批次详情组件：

- 进度环。
- 状态分布。
- 吞吐趋势。
- 失败任务。
- 实例任务表。
- 暂停、恢复和取消剩余任务。

### 20.3 任务历史

筛选：

- 时间。
- 计划。
- 批次。
- 应用。
- 状态。
- 触发方式。
- 数据库类型。

字段：

- 计划时间。
- 开始和结束时间。
- 应用名称。
- `deploy_id`。
- 触发方式。
- 状态。
- 源大小。
- ZIP 大小。
- 文件数。
- 耗时。
- 输出目录。

### 20.4 任务详情

阶段：

1. 入队。
2. 获取租约和实例锁。
3. 身份与权限预检。
4. 解析 appvar。
5. 扫描和数据库检测。
6. 检查网盘空间。
7. 创建 SQLite 快照。
8. 创建 ZIP。
9. 计算 SHA-256。
10. 写入外部 manifest。
11. 更新索引、保留策略和通知。

详情展示：

- 阶段时间线。
- 文件统计。
- SQLite 统计。
- ZIP 统计。
- 资源和吞吐。
- 重试记录。
- 结构化日志摘要。
- 输出路径。

## 21. 备份库

### 21.1 浏览方式

- 按应用：应用 → `deploy_id` → 时间。
- 按时间：日期 → 批次 → 应用。
- 按计划：计划 → 批次 → 快照。

不提供按用户浏览。

### 21.2 快照列表字段

- 快照时间。
- 应用名称和版本。
- `deploy_id`。
- 单实例或多实例。
- 文件数。
- 原始大小。
- ZIP 大小。
- SQLite 数量。
- 完整性状态。
- 最近校验时间。
- 保留状态。

### 21.3 快照详情

展示：

- 应用信息。
- `deploy_id`。
- 计划时间、捕获时间和完成时间。
- 网盘相对路径。
- `snapshot.zip` 大小和 SHA-256。
- 文件索引。
- SQLite 列表。
- 跳过项和警告。
- 来源计划、批次和任务。

操作：

- 快速校验。
- 完整校验。
- 浏览文件索引。
- 导出到恢复目录。
- 查看来源任务。
- 删除到回收站。

### 21.4 文件索引

组件：

- 路径树。
- 文件表。
- 类型筛选。
- 名称搜索。
- 大小筛选。
- 修改时间。
- ZIP 内相对路径。
- 可选文件 SHA-256。

V1 不在线展示文件正文。

### 21.5 导出

将 `snapshot.zip` 解压到当前用户网盘的独立目录：

- 不写入目标应用。
- 不覆盖已有目录。
- 预估空间。
- 校验文件数量。
- 防止 ZIP 路径穿越。

## 22. 存储页

### 22.1 存储概览

- 当前用户 UID。
- 固定备份根目录。
- 已用空间。
- 可用空间。
- 快照数量。
- ZIP 总大小。
- 临时任务数量。
- 最近写入和校验时间。

### 22.2 固定目录结构

```text
LazycatAppBackup/
└── <scheduled_at>/
    └── <deploy_id>/
        ├── snapshot.zip
        └── manifest.json
```

`scheduled_at` 使用 UTC：

```text
yyyyMMdd'T'HHmmss.SSS'Z'
```

同一毫秒发生冲突时，在时间目录后追加批次短 ID。

应用名称、`appid`、版本、实例模式和用户信息写入 `manifest.json`，不加入物理目录层级。

### 22.3 临时目录

任务写入期间使用：

```text
LazycatAppBackup/_partial/<job_id>/
```

失败任务不进入备份库。系统按设置清理过期临时目录。

### 22.4 存储维护

- 立即扫描。
- 清理过期临时目录。
- 执行保留策略。
- 清理回收站。
- 重建快照索引。
- 查看占用最大的应用。
- 校验 ZIP 与 manifest 对应关系。

## 23. ZIP 快照格式

### 23.1 最终文件

每个快照目录只包含：

```text
snapshot.zip
manifest.json
```

### 23.2 ZIP 内容

```text
snapshot.zip
├── appvar/
│   └── 应用数据相对目录
└── _snapshot/
    ├── manifest.json
    ├── file-index.jsonl
    └── warnings.json
```

ZIP 内外各保存一份 manifest。外部 manifest 供备份库快速读取；ZIP 内 manifest 使压缩包单独移动后仍可识别。

### 23.3 完成判定

外部 `manifest.json` 最后写入，且必须包含：

```text
status = completed
archive_name = snapshot.zip
archive_sha256
archive_size
```

备份库只接受：

- 外部 manifest 存在。
- `status` 为 `completed`。
- `snapshot.zip` 存在。
- ZIP SHA-256 与 manifest 一致。

不再使用独立 `COMPLETED` 文件。

## 24. 告警页

### 24.1 告警级别

- 信息。
- 警告。
- 严重。
- 紧急。

### 24.2 告警类型

- OIDC 登录失败。
- 已登录会话 UID 与当前入口账户 UID 不一致。
- 必需权限缺失。
- appvar 投影不可用。
- 应用目录查询失败。
- 单实例共享数据计划提醒。
- 检测到不支持数据库。
- SQLite 在线快照失败。
- 普通文件持续变化。
- 网盘不可写。
- 空间不足。
- ZIP 创建失败。
- ZIP 校验失败。
- 批次超出窗口。
- 连续任务失败。
- 临时目录异常。
- 控制数据库异常。
- 多实例应用未能自动运行导致漏跑。

### 24.3 操作

- 查看关联应用、任务、批次或快照。
- 立即重试。
- 重新检测。
- 查看修复建议。
- 标记已读。
- 确认已处理。
- 临时静默。

## 25. 设置页

### 25.1 账户与登录

- 当前 OIDC 用户。
- UID。
- 角色。
- 会话到期时间。
- 重新登录。
- 退出登录。
- 身份一致性状态。

### 25.2 常规

- 页面语言。
- 默认时区。
- 默认备份时间。
- 默认备份窗口。
- 默认任务超时。
- 是否展示高级设置。

### 25.3 调度与性能

- 扫描并发。
- 普通 ZIP 并发。
- SQLite 快照并发。
- 校验并发。
- 读取限速。
- 写入限速。
- 大型任务阈值。
- 大型任务并发上限。
- 自动重试。
- 漏跑补偿。

### 25.4 备份引擎

- ZIP 压缩级别。
- 已压缩文件是否使用 Store。
- 普通文件严格模式。
- 文件变化重试次数。
- SQLite busy 超时。
- 定期强制完整备份。
- 默认排除规则。

### 25.5 存储

- 当前用户网盘根目录，只读展示。
- 空间告警阈值。
- 临时目录保留时长。
- 回收站保留时长。
- 自动校验周期。
- 自动清理时间窗口。

不提供用户 UID 选择器和任意宿主机路径输入。

### 25.6 通知

- 系统通知。
- 首次失败。
- 连续失败升级。
- 空间不足。
- 超出窗口。
- 校验失败。
- 成功通知。
- 每日摘要。

### 25.7 权限与环境

- `appvar.other.read`。
- `document.write`。
- `user.notify`。
- 兼容权限状态。
- appvar 投影状态。
- 当前用户应用数量。
- LzcOS 版本。
- 应用版本。
- 后台运行状态。
- 最近检测时间。

页面不显示其他用户，不提供跨用户操作。

## 26. 专用状态

### 26.1 无应用数据

文案：

> 该应用当前没有需要备份的运行数据。

操作：重新检测、返回。

### 26.2 数据库不支持

展示：

- 数据库类型。
- 相对路径。
- V1 不支持原因。
- 后续适配计划。

操作：查看详情、重新检测、返回。

不提供强制备份按钮。

### 26.3 单实例共享风险

文案：

> 该应用使用单实例运行，应用内部可能保存共享数据。当前备份范围以懒猫平台向本账号开放的数据目录为准。

操作：

- 我已了解并继续。
- 取消。
- 查看数据范围。

### 26.4 权限或投影异常

展示：

- 缺失权限。
- 投影状态。
- 追踪 ID。
- 重新检测。
- 设置入口。

## 27. 业务流程

### 27.1 应用发现

1. OIDC 登录完成。
2. 后端确认当前 `tenant_uid`。
3. 查询当前用户应用目录。
4. 过滤备份应用自身。
5. 建立应用和 `deploy_id` 记录。
6. 解析当前用户可见 appvar。
7. 执行轻量能力检测。
8. 前端分页展示。

### 27.2 手动备份

1. 用户选择应用。
2. 系统执行完整预检。
3. 单实例时显示共享数据风险提示。
4. 创建批次和任务。
5. 任务进入有界工作池。
6. 创建 SQLite 快照。
7. 普通文件和 SQLite 写入 ZIP。
8. 计算 ZIP SHA-256。
9. 写入 ZIP 内 manifest。
10. 关闭 ZIP。
11. 写入外部 manifest。
12. 快照进入备份库。
13. 发送结果通知。

### 27.3 定时备份

1. 调度器读取到期计划。
2. 重新查询当前用户应用目录。
3. 展开目标并排除不可备份应用。
4. 创建批次和任务。
5. 所有任务保存同一个 `scheduled_at`。
6. 工作池受控执行。
7. 超过窗口时按计划继续、跳过或告警。
8. 执行保留策略。

### 27.4 SQLite

1. 识别标准 SQLite 文件头。
2. 排除原始 DB、WAL、SHM 和 Journal 的普通复制。
3. 在短生命周期临时目录创建 SQLite 一致性副本。
4. 执行 `quick_check`。
5. 将副本写入 ZIP 中原数据库对应的相对路径。
6. 清理临时文件。
7. 失败时整项任务失败。

### 27.5 不支持数据库

1. 扫描发现数据库特征。
2. 任务在创建 ZIP 前终止。
3. 应用状态更新。
4. 产生告警。
5. 不生成成功快照。

### 27.6 应用重启

1. 打开控制数据库。
2. 恢复调度器。
3. 回收过期租约。
4. 对账 `_partial`。
5. 恢复可重试任务。
6. 按补跑窗口处理漏跑计划。
7. 不覆盖历史成功快照。

## 28. 状态定义

### 28.1 可备份性

- `BACKUPABLE`
- `BACKUPABLE_SHARED_WARNING`
- `NO_DATA`
- `UNSUPPORTED_DATABASE`
- `UNKNOWN_DATABASE`
- `SYSTEM_UNSUPPORTED`
- `SOURCE_NOT_READY`
- `SOURCE_PERMISSION_DENIED`
- `SOURCE_INSTANCE_NOT_FOUND`
- `SOURCE_OWNER_MISMATCH`
- `PROBE_FAILED`

### 28.2 任务

- 等待中。
- 已租约。
- 预检中。
- 扫描中。
- SQLite 快照中。
- ZIP 创建中。
- 校验中。
- 提交中。
- 成功。
- 成功但有警告。
- 失败。
- 已取消。
- 已超时。
- 已跳过。
- 异常中止。

### 28.3 快照

- 写入中。
- 已完成未校验。
- 校验通过。
- 校验警告。
- 校验失败。
- 待清理。
- 已移入回收站。

## 29. 非功能要求

### 29.1 安全

- OIDC 登录为必需条件。
- 所有接口校验当前用户身份。
- 不使用 `other_uid`。
- 不接受用户 UID、owner UID 和源绝对路径作为授权参数。
- 源数据只读。
- 不跟随越过源根的符号链接。
- 跳过特殊设备和 Socket。
- 文件正文不进入日志和诊断包。
- ZIP 解压时防止路径穿越。
- 管理员角色不扩大数据范围。

### 29.2 性能

- 列表使用服务端分页和虚拟滚动。
- 单个用户实例支持至少 10,000 条应用实例记录。
- 单批次可以持久化至少 5,000 个任务记录。
- 任务创建不启动同等数量 goroutine。
- ZIP 使用有界缓冲。
- 大文件流式写入。
- SQLite 并发独立限流。
- 前端在 100,000 条任务历史下可以分页浏览。

### 29.3 可靠性

- 计划、批次、任务和租约持久化。
- 任务具有幂等键。
- 失败不删除旧快照。
- 外部 manifest 最后写入。
- ZIP 与 manifest 校验一致。
- SQLite 失败不降级。
- 后端重启后可以恢复队列。

### 29.4 国际化

- 中文和英文覆盖所有页面、状态、错误和通知。
- 后端返回稳定错误码与参数。
- 日期、容量、速度和百分比按语言格式化。
- 切换语言不丢失筛选和表单草稿。

### 29.5 可用性

- 状态同时使用图标和文字。
- 表单有明确错误说明。
- 主要操作支持键盘。
- 移动端支持查看概览、任务、快照和告警。
- 复杂计划编辑以桌面端为主。

## 30. V1 验收标准

### 30.1 登录和隔离

- 未登录时显示懒猫 OIDC 登录页；只有用户点击登录按钮才进入授权，回调成功后进入首页。
- 已登录业务请求中的 `session.gateway_uid` 与 `X-HC-User-ID` 一致。
- 普通用户只能看到自己的应用、任务和快照。
- 管理员无法查询其他用户。
- 不存在用户切换功能。
- 备份结果只写入当前用户网盘。

### 30.2 数据获取

- 当前已跑通的应用目录、appvar 投影、探测和手动备份链路继续工作。
- 支持当前用户可见的单实例和多实例应用。
- 单实例显示共享风险。
- 能备份普通文件和目录。
- 能创建 SQLite 一致性副本。
- 已知不支持数据库被阻断。
- 无数据应用不创建空快照。

### 30.3 ZIP 和目录

- 归档格式为 ZIP。
- 最终路径为：

```text
LazycatAppBackup/<时间>/<deploy_id>/snapshot.zip
LazycatAppBackup/<时间>/<deploy_id>/manifest.json
```

- ZIP 内含 `appvar/` 和内部 manifest。
- 外部 manifest 最后写入。
- ZIP SHA-256 与 manifest 一致。
- 失败任务不进入备份库。

### 30.4 页面

- 八个主菜单和全部详情页可用。
- 登录、会话过期和身份错误状态可用。
- 应用列表不包含其他用户。
- 单实例、多实例、无数据、数据库阻断和权限异常均有明确状态。
- POC 探测页面不出现在正式导航。

### 30.5 调度

- 支持立即、每天、每周和 Cron。
- 支持批量目标。
- 同一个 `deploy_id` 不会并发运行两个备份。
- 队列受资源限制。
- 重启后可以补跑。

### 30.6 中英文

- 中文和英文功能一致。
- OIDC 登录错误、权限、任务和通知均完成翻译。

## 31. 后续功能

- 管理员跨用户备份，需独立权限、授权和审计。
- 一键恢复组件，独立申请写入权限。
- MySQL、PostgreSQL、MongoDB、Redis 适配器。
- 应用 `prepare/export/complete` 备份协议。
- Notus 专用索引 flush 适配器。
- S3、WebDAV、SFTP、NAS 和异地微服。
- 客户端加密与密钥导出。
- 增量 ZIP、内容寻址和去重。
- 自动恢复演练。
- 懒猫网盘、相册和 LightOS 官方接口适配。

## 32. 参考资料

- [懒猫 OIDC](https://developer.lazycat.cloud/advanced-oidc.html)
- [懒猫 HTTP Headers](https://developer.lazycat.cloud/http-request-headers.html)
- [懒猫多实例](https://developer.lazycat.cloud/advanced-multi-instance.html)
- [懒猫开发者环境变量](https://developer.lazycat.cloud/advanced-envs.html)
- [package.yml 权限规范](https://developer.lazycat.cloud/spec/package.html)
- [lzc-manifest.yml 规范](https://developer.lazycat.cloud/spec/manifest.html)
- [文件访问](https://developer.lazycat.cloud/advanced-file.html)
- [当前项目仓库](https://github.com/dnwwdwd/lazycat-app-snapshot)
- [SQLite Online Backup API](https://sqlite.org/backup.html)
- [Go archive/zip](https://pkg.go.dev/archive/zip)

## 33. 变更记录

| 日期 | 文档版本 | 变更 |
| --- | --- | --- |
| 2026-08-27 | V1 | 参考 `agent-desk` 的会话模型修正身份判断：OIDC `preferred_username` 只保存为 profile UID；回调保存 `X-HC-User-ID` 为 `session.gateway_uid`；`IDENTITY_MISMATCH` 仅表示该字段与当前请求 Header 不一致。 |
| 2026-08-27 | V1 | 正式子域名使用 `mimi-app-backup`，POC 使用 `mimi-app-backup-poc`；已安装实例需通过升级或重装新的 LPK 才会获得新的平台分配域名。 |
| 2026-08-27 | V1 | 产品更名为“咪咪应用备份（Mimi App Backup）”，LPK 包标识改为 `mimi-app-backup`，前端菜单与登录页同步更新；本地 LPK 产物为 `mimi-app-backup-0.1.0.lpk`。 |
| 2026-08-27 | V1 | 登录入口采用显式 OIDC 登录页：用户点击后才创建授权事务，回调成功后进入首页；网关 UID 不匹配会清除旧会话并回到登录页。 |
| 2026-08-27 | V1 | 阶段 5 完成本地实现：首次使用检查、概览、完整计划编辑、任务与批次详情、备份库和文件索引、存储维护、站内告警、当前用户设置、审计、限时 SSE 与中英文文案全部改用正式 API。外部 `user.notify`、OIDC 回调、网关 UID 会话绑定、A/B 用户隔离、网盘写入和后台补跑仍待真实平台验收；该阶段记录不包含 LPK 打包。 |
| 2026-08-27 | V1 | 完成阶段 4 本地实现：计划、批次、任务队列、租约恢复、重试和补跑接入正式 API；备份库支持文件索引、完整校验、导出、回收站、保留和存储维护。完整计划向导、告警、SSE、设置和中英文完整界面仍待后续阶段。 |
| 2026-08-27 | V1 | 完成当前用户单实例手动备份的本地实现：应用页可发起备份并确认共享风险，备份库显示真实快照和快速校验；真实平台网盘写入及 A/B 隔离仍待验收。 |
| 2026-08-27 | V1 | 重写用户模型、OIDC、单/多实例支持、管理员边界、ZIP 格式和网盘目录 |
